# Auto VWAP

트레이딩뷰용 Pine Script 오버레이 지표 설명서입니다.

대상 스크립트:
- [`auto-vwap.pine`](./auto-vwap.pine)

이 지표는 VWAP 누적을 **`최근 N봉 롤링(Recent Bars)` 단일 방식**으로 계산합니다(CEO 지시 — 앵커모드 제거). 이전에는 `현재 세션 기준 VWAP`·`최근 N거래일 기준 VWAP`·`최근 N봉 기준 VWAP`를 앵커모드로 바꿔 볼 수 있었으나, 알고리즘 수학자가 지적한 DRIFT(원본 이치모쿠의 `vwap상태(20)`는 앵커 무관 Recent Bars 고정이 정답) 해소를 위해 **앵커모드 입력·세션/거래일 분기·세션 리셋 로직을 전부 삭제**하고, 전 VWAP(화면 50봉·`vwap상태(20)`·중기추세가 쓰는 `vwapValue`·지지저항 등)이 Recent Bars 단일로 계산되게 했습니다. 핵심은 `평균 체결 단가가 어디인지`, 그리고 `현재 가격이 그 기준에서 얼마나 멀리 벗어났는지`를 빠르게 읽는 데 있습니다.

## 현재 소스 기준으로 보이는 것

| 요소 | 현재 표시 방식 |
| --- | --- |
| VWAP | 선택한 기준 구간의 거래량 가중 평균 단가입니다. |
| Band #1 | VWAP 대비 1차 확장 구간입니다. |
| Band #2 | VWAP 대비 2차 확장 구간으로, 과확장 감시에 더 가까운 선입니다. |
| 밴드 계산 타입 | `Log Standard Deviation`, `Absolute Standard Deviation` 중 선택합니다. |
| 누적 방식(고정) | **최근 N봉 롤링(Recent Bars) 단일 방식**입니다(CEO 지시 — 앵커모드 제거). 세션/거래일 앵커 분기·세션 리셋 로직은 삭제됐고, VWAP는 항상 최근 `N`봉 누적 구간을 하나의 이동 창처럼 계산합니다. 세션 경계를 무시하고 현재 타임프레임의 봉 수 기준으로 바로 계산합니다. |
| VWAP 세트 | VWAP·밴드·밴드2는 **단일 화면 세트**로 계산합니다 — window 길이 `Recent Bars = 50`(기본), 소스 `Source`(기본 `hlc3`), 화면 plot·밴드·라벨의 기준입니다. VWAP 계산 헬퍼(`computeVwapSet`)가 window 길이(롤링 길이)와 소스를 인자로 받아 **Recent Bars 롤링(앵커 = `barsLen`봉 전 누적)** 으로 확정봉 기준 누적하므로 리페인팅이 없습니다. |
| 선 굵기·가격축 | VWAP 중앙선은 **2px**이며(CEO 지시 — 지지/저항 색 규칙). 색 구성은 VWAP 값을 두 plot으로 나눠, 동적 색 선(녹/빨강/흰)과 흰색 고정 가격축 라벨을 분리하는 구조입니다(선은 pane에 1회만 그려져 중복선 없음). **VWAP 중앙선의 가격축 라벨은 선색과 무관하게 항상 흰색으로 고정**되도록 별도 plot(제목 `VWAP 가격축 라벨`)으로 흰색만 담당하며, 지지/저항 색 옵션 ON/OFF와 무관합니다. ⚠️ **기본 표시(Style 탭) 상태는 아래 "기본 표시 옵션" 행 참조** — VWAP 본선·VWAP 가격축 라벨·Band #1은 CEO 지시로 **기본 OFF**입니다(색·산식·판정 로직은 불변, plot의 `display` 인자만 조정). |
| 기본 표시 옵션(Style 탭 기본 체크) | 차트에 지표를 처음 얹었을 때의 **기본 표시 상태**입니다(사용자가 Style 탭·설정에서 각자 켜고 끌 수 있음, 값·조건·색·산식은 전부 불변 — plot의 `display` 인자만 설정). **기본 OFF(`display.none`)**: **브이왑(VWAP 본선)**·**VWAP 가격축 라벨**·**Upper Band #1**·**Lower Band #1**(CEO 지시). Band #1은 plot의 `display`를 `display.none`으로 직접 고정했으며, `showBand_1` 입력 토글과 밴드1 %갭 라벨(`showBand_1` 조건) 로직은 불변입니다. Band #2·리본·SAR·이격도 등 다른 plot의 기본 표시는 변경하지 않았습니다. |
| VWAP 선 지지/저항 색 | `VWAP 선 지지/저항 색(구간 종가·꼬리 걸침 이탈)` 옵션(기본 **ON**)이 켜져 있으면 화면 VWAP 중앙선(`vwapValue`)의 색을 **봉 단위**로 칠합니다(CEO 확정 규칙). **"near 구간"** = VWAP와 밴드1 사이의 **동적 %(pct) 범위**입니다(밴드1 전체가 아님). 하단 near `[lowerNear, vwap]`, 상단 near `[vwap, upperNear]`. **`upperNear = vwap + pct×(upperBand1 − vwap)`**, **`lowerNear = vwap − pct×(vwap − lowerBand1)`** — 저항(하단)·지지(상단) **동일 pct 대칭**. **`pct`(구간 비율)는 밴드폭 반비례 동적**입니다: 밴드폭이 **좁을 때(저변동성) 최대 0.60(60%)**, **넓을 때(고변동성) 최소 0.20(20%)**로 수렴합니다. 산식은 `bandWidth1 = upperBand1 − lowerBand1`, `r = bandWidth1 / vwap`(가격 대비 밴드폭 비율, 종목·가격 무관), `t = clamp((r − rLow)/(rHigh − rLow), 0, 1)`, `nearPct = maxPct − t×(maxPct − minPct)`, `pct = clamp(nearPct, 0, 100)/100`입니다 — `r ≤ rLow`(저변동성·좁은밴드)면 `maxPct`(넓은 near), `r ≥ rHigh`(고변동성·넓은밴드)면 `minPct`(좁은 near), 사이는 선형보간(반비례). 기본값은 `VWAP 근처 구간 최소 %(넓을 때)=20`·`최대 %(좁을 때)=60`·`저변동성 임계=0.5%`·`고변동성 임계=2.0%`입니다(임계 뒤집힘·`vwap==0`이면 `t=0` → 최대% 폴백). 색 규약: **지지=녹 / 저항=빨강 / 중립=흰**. 규칙은 다음과 같습니다 — **저항=빨강**(`UPPER_BREAK_LINE_COLOR`) = **① `lowerNear ≤ close ≤ vwap`**(하단 near 안에 종가 마감 = VWAP이 저항) **OR ② `close < lowerNear` AND `high > lowerNear`**(종가는 하단 near 아래로 이탈했지만 **고가**가 하단 near 최저 위 = 꼬리 걸침 되밀림); **지지=녹**(`LOWER_BREAK_LINE_COLOR`) = **③ `vwap ≤ close ≤ upperNear`**(상단 near 안에 종가 마감 = VWAP이 지지) **OR ④ `close > upperNear` AND `low < upperNear`**(종가는 상단 near 위로 이탈했지만 **저가**가 상단 near 최고 아래 = 꼬리 걸침 되밀림); **그 외**(완전 이탈: `close`가 near 구간 밖이고 꼬리도 near에 안 걸침 / 워밍업 `na`) → **중립=흰**(`DEFAULT_LINE_COLOR`). **`close == vwap` 경계**: ①은 `close ≤ vwap`, ③은 `close ≥ vwap`이라 `close == vwap`이 양쪽에 걸립니다 — **저항을 먼저 평가**(코드에서 `if 저항 → else if 지지`)해 `close == vwap`이면 **저항=빨강**으로 확정합니다(부동소수라 실제 동일값은 드묾). **상호배타·미정의 구멍 없음**: ②(`close<lowerNear`)와 ①(`close≥lowerNear`) 배타, ④(`close>upperNear`)와 ③(`close≤upperNear`) 배타, 저항 계열(`close≤vwap` or `close<lowerNear`)과 지지 계열(`close≥vwap` or `close>upperNear`)은 `close==vwap` 외엔 배타이며 `close==vwap`은 위 우선순위로 저항 단일 확정 → 전 조합이 정확히 한 색으로 결정됩니다. **②④의 의미**: ②는 "종가가 하단 near 아래로 이탈했지만 아래꼬리가 아니라 **위꼬리(고가)가 하단 near에 걸쳐 되밀린** 봉"을 저항으로, ④는 "종가가 상단 near 위로 이탈했지만 **저가가 상단 near에 걸쳐 되밀린** 봉"을 지지로 인정합니다(순수 종가 이탈이라도 꼬리가 near를 관통했으면 지지/저항 신호로 봄). **엣지케이스**: 밴드1 편차 0(완전 도지)이면 `bandWidth1=0` → `r=0(≤rLow)`이라 pct=최대나 `upperNear=lowerNear=vwap`로 near 구간이 소멸 — 종가가 그 값이면 ① 해당(저항), 아니면 꼬리(high/low)로 ②④ 판정. 이 색은 봉 단위 series color라 **과거 봉은 값이 확정돼 리페인팅이 없고**, 형성 중 현재봉만 종가/고저 확정 전 색이 바뀔 수 있다가 봉 마감 시 확정됩니다(과거 변조 아님). 판정에 쓰는 `close`·`high`·`low`·`vwap`·밴드1·near는 모두 확정봉 세트값입니다. 색 우선순위: 지지/저항 규칙이 ON이면 이 규칙이 VWAP 선 색을 담당하고, OFF이면 아래 `Enable Dynamic Line Color` 거동(밴드2 이탈 녹/빨강, 기본 흰색)을 따릅니다. |
| 선 색상 변화(밴드2 이탈) | `Enable Dynamic Line Color`를 켜면 `Band #2` **이탈 시** 하단 아래 녹색·상단 위 빨간색으로 읽습니다(기본 흰색). 이 옵션은 `Band #1`에 적용되며, VWAP 선에는 위 `VWAP 선 지지/저항 색`이 **OFF일 때만** 적용됩니다(ON이면 지지/저항 규칙이 우선). 두 색 규칙은 서로 독립입니다. |
| 최외곽 선(`Band #2`) 색·두께 | `Upper/Lower Band #2`(±2σ, 최외곽 선)는 **위치가 항상 밴드2 원값(VWAP±2σ)에 고정**되며(이치모쿠 밴드처럼 위치 불변), 색은 **기본 단색(`activeLineColor`)** 입니다. 선 두께는 **기본 1px 대시(dashed)**입니다. 위 `Enable Dynamic Line Color`(녹/빨강 이탈 표시)는 VWAP 선·`Band #1`에 적용됩니다. |
| 파라볼릭 SAR (화면 표시) | **SAR을 화면에 십자(`plot.style_cross`)로 표시**합니다 — **색: SAR이 종가 아래(상승, `sarBelow`)=녹(`LOWER_BREAK_LINE_COLOR`, #00FF00)·SAR이 종가 이상(하락, `sarAbove`)=빨강(`UPPER_BREAK_LINE_COLOR`, #FF0000)**([C] 종가 기준). ⚠️ 청산선 제거(CEO 지시)로 기존 SAR 색이 참조하던 청산선 색(롱/숏 청산선 색)이 삭제되어, 상단에 이미 선언된 **지지/저항 색 상수(녹=상승·빨강=하락)로 대체**했습니다(의미 일치·잔존 참조 0). `sarVal`이 유효하면 종가 기준 이분(`< / >=`)이라 항상 한쪽 색이 잡히고, `sarVal`이 `na`인 워밍업에서만 미표시. **경계 배정**: `sar == close`는 **하락(`sarAbove`, `>=`) 쪽에 포함**해 두 조건이 상호배타·전영역을 덮어 `na`(둘 다 false)가 나오지 않게 했습니다(부동소수 특성상 정확 일치는 극히 드묾). **가격축 라벨 없음(CEO 지시)**: SAR 십자 plot은 `display.all − display.price_scale`로 그려, 차트 십자는 그대로 두되 우측 가격축의 숫자 라벨만 끕니다(색·스타일·offset 불변). SAR 파라미터는 `ta.sar(start, increment, maximum)`이며 **Start 기본 0.03·Increment 기본 0.03·Max 기본 0.3**(가속을 통상값 0.02/0.02/0.2보다 빠르게 잡음). `ta.sar`은 확정봉 기반 누적 상태 함수라 과거봉 값이 확정되어 **신규 리페인팅이 없습니다**. **표시 전용**입니다. |
| 비정상 매수/매도 배경 | `비정상 매수/매도 배경 표시` 옵션(기본 **ON**)이 켜져 있으면, **과매수/과매도 구간을 화면 전체 세로 배경**으로 칠합니다(SUPER-ICHIMOKU 메인 지표 `advanced-ichimoku.pine`의 "비정상 캔들" 판정 이식). ⚠️ **렌더 방식(`bgcolor`로 복원 — auto-scale 폭발 버그 수정, CEO 지시)**: 직전 z-order 작업에서 이 배경을 `bgcolor()`에서 **plot 기반 `fill`로 전환**한 적이 있습니다 — 페인 상/하단을 잇는 보이지 않는 무한대 기준선 2개(`plot(±1e20, display=display.none)`) 사이를 `fill`로 채우는 방식이었고, "`display.none` plot은 auto-scale에 무영향"이라는 정적 가정에 근거했습니다. 그러나 **실측(차트)에서 그 `fill`이 ±1e20 범위로 렌더되어 가격축 auto-scale을 ±수경(≈4.4e35)대로 폭발**시켜 캔들이 일직선으로 뭉개지는 심각한 버그가 확인됐습니다(정적 가정이 실측에서 틀림). 그래서 **극단값 plot 2개와 그 `fill`을 제거하고 다시 `bgcolor()`로 복원**했습니다(스케일에 영향 없는 유일한 화면 전체 배경). **색·조건·투명도·판정(`abnBg`/`abnShowBg`/`abnCandleState` 래치)은 전부 불변, 렌더 방식만 `fill`→`bgcolor`로 환원**(bgcolor일 때와 표시 픽셀 동일 — 전체 화면 세로 스트립·같은 색·같은 투명도). **z-order(레이어) — 제약 인지**: `bgcolor`는 `explicit_plot_zorder`와 무관하게 **항상 최후면 그룹**이라, 복원 후 실제 레이어는 (뒤→앞) **[비정상 배경 `bgcolor`(가장 뒤)] → [거래량 가중선 50/100/200+fill] → [나머지 페인팅(VWAP·밴드·정/역배열 배경·리본·SAR·프랙탈/%R 마커)]** 입니다. 즉 비정상 배경은 이전 fill 방식에서 노렸던 "중간"이 아니라 **"가장 뒤"** 가 됩니다(Pine 구조상 auto-scale과 양립하기 위해 불가피 — 아래 "레이어(z-order) 순서" 항 참조). **판정 = cRSI 과매수/과매도(도미넌트 사이클) + 볼린저 밴드 이탈(고가/저가 기준)** 두 조건 AND. **비정상 매수(=과매수)** = `cRSI ≥ 절대 과매수(70)` **AND** 고가가 볼린저 상단 밴드를 초과(초과폭 ÷ 캔들 범위 ≥ `비정상 밴드 초과 비율`) → **주황 배경(`#FF9800`)**(CEO 지시 — 리본 역배열 배경 빨강 `#C62828`과 혼동 방지). **비정상 매도(=과매도)** = `cRSI ≤ 절대 과매도(30)` **AND** 저가가 볼린저 하단 밴드를 이탈(이탈폭 ÷ 캔들 범위 ≥ 비율) → **파랑 배경**(CEO 명시: 매도=파랑·매수=주황, 방향 직관과 반대지만 지시대로). 상태는 **래치**되어 다음 비정상 봉까지 유지되며, **이격도(Disparity)가 중립(되돌림 주의·확실 모두 해소)으로 복귀하면 리셋**됩니다(원본과 동일한 종료 조건). ⚠️ **자기완결 이식**: 원본은 기준 길이를 이치모쿠 베이스 라인(`basePeriods`)에 연동했으나 auto-vwap엔 그 input이 없어, **CEO 지시로 비정상 전체 기준 길이를 단일 input `비정상 기준 길이(볼린저 등)`(기본 20) 하나로 통일**했습니다(구 전용 input `비정상 기준 길이(베이스 라인 대응)`(기본 22)는 삭제). 이 값 하나에 **볼린저 밴드 길이 + cRSI 사이클(이 값÷2=10)·ATR·RSI·반등 추적 lookback**이 모두 연동됩니다(전 기준 길이 22→20으로 통일). 리셋 트리거인 **이격도 계산도 이치모쿠 원본 산식·input(`이격도` 그룹: MA 종류·길이·임계·통계 구간·σ 배수) 그대로 이식**했습니다(이격도는 비정상 상태 래치 리셋에만 쓰이며 테이블·별도 표시를 새로 만들지 않음). 그 외 판정 파라미터(cRSI Vibration·절대 과매수/과매도·볼린저 배수·밴드 초과 비율)·배경색·투명도(기본 82)는 원본 명칭/기본값과 동일합니다. 배경은 확정 성격 래치(`abnCandleState`) 기반이라 원본과 동일한 리페인팅 특성입니다(형성 중 현재봉만 실시간 변동, 과거봉 확정 — 렌더 방식을 fill로 바꿔도 판정·리페인팅 특성은 불변). **정배열/역배열 배경(밴드2 영역 한정 fill)과 시각 충돌 없음**: 비정상 배경은 화면 전체 세로 스트립이며 반투명(기본 82로 더 옅음)이라 밴드2 영역 fill과 겹쳐도 각자 반투명 합성될 뿐 덮어쓰지 않고, 색(파랑/주황 vs 녹/빨강)으로도 구분됩니다(비정상 매수 배경을 주황 `#FF9800`으로 두어 리본 역배열 빨강과 혼동을 없앰). ⚠️ z-order상 정/역배열 배경은 비정상 배경보다 **앞**(나중 선언)이지만 둘 다 반투명이라 겹치는 밴드2 영역에서 합성될 뿐 판정·색은 서로 독립입니다. **표시 전용**(밴드·정배열/역배열 배경 판정, 매매 전략과 무관). |
| 지수 추세 리본(EMA/DEMA/TEMA) | `지수 추세 리본 표시(EMA/DEMA/TEMA)` 옵션(기본 **ON**)이 켜져 있으면, **close(종가) 기준 EMA·DEMA·TEMA 3선**을 오버레이로 그립니다(각 **1px**). 계산은 단일 소스 close에 EMA를 3중 누적한 `e1 = EMA(close, 지수 리본 길이)`·`e2 = EMA(e1, …)`·`e3 = EMA(e2, …)`에서 **`EMA = e1`, `DEMA = 2·e1 − e2`, `TEMA = 3·e1 − 3·e2 + e3`**로 파생합니다. **길이 기본 20**(`지수 리본 길이`). 색은 옵션(**EMA=노랑 / DEMA=하늘색 / TEMA=분홍**)이며 자유롭게 바꿀 수 있습니다. DEMA/TEMA는 EMA 지연을 줄인 지수형 추세선으로, 세 선의 정배열(TEMA>DEMA>EMA)/역배열 위치·기울기가 추세 국면을 압축해 보여줍니다. **우측 가격축 라벨 없음**(`display.all − display.price_scale` — 차트 선만, 가격축 숫자 라벨 끔). `ta.ema` 누적(확정 성격)이라 **신규 리페인팅이 없습니다**. ⚠️ **화면에 그려지는 것은 이 종가 기준 3선뿐**이며, 아래 정배열/역배열 배경 판정에 쓰는 저가/고가 기준 리본은 **계산 전용(plot 없음)**으로 이 3선과 별개입니다. **표시 전용**(매매 판정·밴드와 무관). |
| 정배열/역배열 배경 | `정배열/역배열 배경 표시` 옵션(기본 **ON**)이 켜져 있으면, **배경 판정 전용 리본**(계산만·화면 표시 없음)의 **직전봉 대비 기울기 크기 순서**로 정배열/역배열을 판정해 밴드2(상단2~하단2) 영역을 **정배열=녹(#2E7D32)·역배열=빨강(#C62828)** 반투명 배경(기본 투명도 **80**)으로 채웁니다. **판정(CEO 재설계 — 저가/고가 기준 2세트 리본)**: 화면에 그려지는 EMA/DEMA/TEMA 3선(종가 기준)과 **별개로**, 배경 판정 전용 리본 2세트를 계산합니다(plot 없음). **저가 기준**(`emaLow = EMA(low, len)`, `demaLow = 2·emaLow − EMA(emaLow)`, `temaLow = 3·emaLow − 3·EMA(emaLow) + EMA(EMA(emaLow))`)·**고가 기준**(`high` 소스로 동일 → `emaHigh`/`demaHigh`/`temaHigh`). 산식은 종가 리본과 동일하고 소스만 다릅니다. **녹(정배열)=저가 기준 리본** 각도 `sTemaLow > sDemaLow and sDemaLow > sEmaLow`(직전봉 대비 기울기 크기 순서, **부호 무관**), **빨강(역배열)=고가 기준 리본** 각도 `sTemaHigh < sDemaHigh and sDemaHigh < sEmaHigh`(부호 무관) — **각도 체크 순서 로직은 종전과 불변**(부호 조건 없는 순서 비교 그대로)이고, 소스만 녹=저가/빨강=고가로 분리했습니다. 녹·빨강이 **서로 다른 리본**(저가 vs 고가)이라 **동시 성립할 수 있으며**, 이 경우 **확정봉 봉 색 + 꼬리**로 배경색을 결정합니다: `ribbonState = greenRaw and redRaw ? (greenPick ? 1 : redPick ? −1 : 0) : greenRaw ? 1 : redRaw ? −1 : 0` — **양봉(`c1>o1`): 윗꼬리≤몸통→녹(1)/윗꼬리>몸통→빨강(−1, 상방 거부) / 음봉(`c1<o1`): 아래꼬리≤몸통→빨강(−1)/아래꼬리>몸통→녹(1, 하방 거부) / 도지(`c1==o1`)→중립(0·배경 없음)**. `body1=|c1−o1|`, `upWick1=h1−max(c1,o1)`, `dnWick1=min(c1,o1)−l1`("길면"=초과 `>`·"이하"=`<=`, 도지는 `greenPick`·`redPick` 모두 false→0). 봉 색·꼬리·몸통 모두 배경 기울기 판정과 동일한 **확정봉 `[1]` 기준**(`o1/c1/h1/l1`)이라 형성 중 현재봉 변동에 따른 점멸이 없습니다. `1`=녹/`−1`=빨강/`0`=중립. **밴드1 이탈 + 긴 꼬리 억제 게이트(CEO 지시)**: `ribbonState` 확정 뒤, **녹(1)**은 `high[1] > upperBandValue1[1]`(고가가 밴드1 상단 위) AND `upWick1 > body1`(윗꼬리 > 몸통)이면, **빨강(−1)**은 `low[1] < lowerBandValue1[1]`(저가가 밴드1 하단 아래) AND `dnWick1 > body1`(아래꼬리 > 몸통)이면 그 봉을 **중립(0·배경 미표시)** 으로 뺍니다(`ribbonState := (ribbonState==1 and greenSuppress) or (ribbonState==−1 and redSuppress) ? 0 : ribbonState`). 녹/빨강 전체(동시+단독)에 적용하며, 밴드1`[1]`/`high[1]`(녹)·`low[1]`(빨강)이 `na`(워밍업)면 억제하지 않습니다(안전 기본). 확정봉 `[1]` 세트값이라 신규 리페인팅 없음. 세 선이 같은 가격축·같은 단위(slope)라 **순서 비교만으로 충분**해 **ATR 정규화가 불필요**합니다. **리페인팅 처리(CEO 권장 = 확정봉 기준)**: 세 리본(종가/저가/고가) 모두 기울기를 **직전 확정봉 기준**(`[1]` 대비 `[2]`, 예 `sTemaLow = temaLow[1] − temaLow[2]`)으로 계산해 형성 중 현재봉 변동이 판정에 들어오지 않아 **배경 점멸이 없습니다**(과거봉은 `ta.ema` 누적이라 불변 → 신규 리페인팅 없음, 배경은 확정봉 기준으로 한 봉 지연 반영). 워밍업 봉에서 `[2]`가 `na`면 차분이 `na`→부등식 false로 배경 미표시(`slopeValidLow`/`slopeValidHigh` 가드). 배경은 **밴드2 영역 한정 fill**(전체 화면 아님)이며 밴드2 선(plot) 색은 불변입니다(`shouldPlot`·밴드2 `na` 가드 유지). **입력**(`지수 추세 리본` 그룹): `정배열/역배열 배경 표시`(ON)·`정배열 배경색`(#2E7D32)·`역배열 배경색`(#C62828)·`배경 투명도`(80). **표시 전용**. |
| 거래량 가중선(VWMA 50/100/200) + 100~200 fill | SUPER-ICHIMOKU 메인 지표(`advanced-ichimoku.pine`)에서 이식한 **거래량 가중 이동평균선**입니다(매매 판정 아님, 표시·추세 참고선). **50선**(`거래량 가중 50선`, 기본 **ON**, **2px**)은 `ta.vwma(close, 50)`이며 색은 **딥 오렌지(`#FF6D00`)** 입니다 — 원본은 노랑(#FFEB3B)이나 auto-vwap **리본 EMA(노랑)와 겹쳐**, 리본(노랑/하늘색/분홍)·100선(파랑)·200선(보라)·신호색(녹/빨강)·비정상 배경(파랑/주황)과 **확실히 구분되는 색**으로 바꿨습니다(CEO 지시, `거래량 가중 50선 색`으로 조정 가능). **100선·200선**(`장기선 100/200 표시`, 기본 **ON**, 각 **2px**)은 `장기선 MA 종류(100/200)`(기본 **VWMA**)에 따라 `ta.vwma/sma/ema/wma(close, 길이)`로 계산하며(길이 기본 100·200, 색 기본 파랑·보라), **100선과 200선 사이를 fill**(색: 100선>200선이면 100선 색, 아니면 200선 색; `100~200선 fill 투명도` 기본 75)합니다. 세 선 모두 **우측 가격축 라벨 없음**(`display.all − display.price_scale`)이고 `ta.vwma`/`f_ltMa` 확정봉 기반이라 **신규 리페인팅이 없습니다**. ⚠️ **z-order(레이어) — plot/fill 중 가장 뒤**: 이 50/100/200선과 100~200 fill을 **모든 `plot`/`fill` 중 가장 뒤**에 그립니다(`explicit_plot_zorder=true` + 파일에서 가장 먼저 선언). 그 앞에 나머지 페인팅(VWAP·밴드·정/역배열 배경·리본·SAR·프랙탈/%R 마커)이 옵니다. ⚠️ **단, 비정상 매수/매도 배경(`bgcolor`)은 `explicit_plot_zorder`와 무관하게 항상 최후면 그룹이라 이 VWMA선보다도 더 뒤(가장 뒤)에 그려집니다**(auto-scale 폭발 버그로 fill→bgcolor 복원한 결과 — 위 "비정상 매수/매도 배경" 행·아래 "레이어(z-order) 순서" 항 참조). 계산·plot의 산식·색·오프셋·`display`는 이동 전과 동일하며 **선언 위치(z-order)만** 앞으로 옮겼습니다. ⚠️ **텐칸·기준·후행 등 다른 이치모쿠 페인팅은 이식하지 않았습니다**(지시 — 이 세 선과 100~200 fill만). |
| 상태 테이블(우측 상단) | `상태 테이블 표시(우측 상단)` 옵션(기본 **ON**)이 켜져 있으면, 화면 우측 상단에 상태 테이블을 표시합니다(SUPER-ICHIMOKU 테이블 이식, **판정식은 원본 로직 그대로**·확정봉/오프셋 불변). 3개 섹션입니다 — **① 추세 확인**: `단기추세`(20선 vs 50선 VWMA 배열·크로스), `중기추세`(화면 VWAP 50 vs 100선), `장기추세`(100선 vs 200선). 각 셀 = `상승/하락 (±변화율% \| N봉)`, 골든/데드 크로스·강약(폭 변화)·상승계열 청록(`#0E9C8A`)·하락계열 자홍(`#C2185B`). **② 가격 구조 및 변동성 확인**(원본 "캔들 및 되돌림 확인"에서 개명): 기존 `vwap 상태 (20)` 단일 행(`기울기 \| 확장/수축(밴드폭%) \| 위치상태`)을 **2개 행으로 분리**했습니다(CEO 지시 — 판정 산식 6상태·slope·밴드워킹·이탈·중앙공방·σ·밴드2 전부 불변, 표시 분리만). ⓐ `vwap 기울기`(섹션 **첫 행**, 별도 20봉 VWAP·밴드 6상태 중 **1·2번째 값** = `기울기 \| 확장/수축(밴드폭%)`, 배경색은 기울기 성격만 반영 — 상승=둔탁 녹(`#6E8F72`)·하락=둔탁 빨강(`#A57A7A`)·평탄=회색). ⓑ `vwap 구간`(둘째 행, **3번째 값**인 밴드 국면 = 상단/하단 이탈·상단/하단 밴드워킹·상단 저항·하단 지지·중앙 공방, 배경색은 기존 vwap상태 밴드색 그대로). ⓑ 값 뒤 괄호에 **현재 종가의 %갭(화면 VWAP 밴드 선 끝 %라벨과 동일 기준)**을 붙입니다(CEO 지시로 산식 교체 — 이전의 `(close−center)/(밴드2−center)×100` 20봉 밴드 내 % 위치 방식은 폐기). **화면에 그려지는 vwap 밴드 선들의 오른쪽 끝 %갭 라벨**(밴드1/밴드2 라벨, `formatPercentGap(밴드값, vwapValue) = (밴드값/vwapValue − 1)×100`, "+0.00%" 포맷)과 **동일 산식·동일 세트**로, `close`를 대입해 `formatPercentGap(close, vwapValue) = (close/vwapValue − 1)×100`을 계산합니다. → **종가가 특정 밴드선에 머물면 그 밴드 라벨과 같은 %가 나옵니다**(예: 밴드1 선 끝 라벨이 "+5%"인데 종가가 그 선에 있으면 괄호도 "+5%"). ⚠️ **세트 선택(CEO 보고 대상)**: 화면 밴드 %라벨은 20봉(vwap상태)이 아니라 **화면 50봉 세트(`vwapValue`, `Recent Bars=50`·`hlc3`)**를 씁니다. CEO가 지목한 "화면 밴드 선 끝 라벨"과 일치시키기 위해 이 괄호 %도 그 **50봉 세트(`vwapValue`)** 기준으로 계산합니다(상태 텍스트는 여전히 20봉 6상태 — 괄호 % 산식만 50봉 %갭으로 교체). 부호·자리수는 밴드 %라벨과 동일(소수 2자리, 양수 앞 "+", 예: `중앙 공방 (+0.42%)`). 화면 VWAP 유효성(`shouldPlot`·`not na(vwapValue)`)·`formatPercentGap` 내부 na/분모0 가드 시 % 미표기. `비정상 매수/매도`(기존 abnCandleState/abnWatch 래치 재사용), `이격도 (20)`(기존 disparity 재사용), `되돌림 가격`(이격도 중립 복귀 가격). **③ 지지·저항 확인**: `50선 지지/저항`·`100선 지지/저항`·`200선 지지/저항` — **세 행 모두 화면에 페인팅되는 거래량 가중선(50선=`ta.vwma(close,50)` 주황·100선=`ltMa1`·200선=`ltMa2`)을 기준으로**, 각 선 ± `σ_vw×배수`(`f_vwStdev`·`srBandStdMult`)로 만든 판정 전용 밴드로 지지/저항을 평가합니다(세 행 동일 패턴). ⚠️ **`50선 지지/저항`은 페인팅 50선(`vwma50`)과 정합**합니다 — 이전에는 이 행이 화면 VWAP 중앙선(`vwapValue`, 롤링 50봉 VWAP·`hlc3`)과 그 밴드1을 참조해 페인팅 50선(주황)과 **다른 값**으로 판정하던 버그가 있었고, CEO 리포트로 페인팅 50선(`vwma50`) 기준으로 일원화했습니다(판정식·near%·σ 배수는 불변, 입력 MA만 통일). 각 지지/저항 셀 = `상태 (이격도 \| N봉)`, 지지=녹·저항=빨강·단순 위=둔탁 녹(`#6E8F72`)·단순 아래=둔탁 빨강(`#A57A7A`). ⚠️ **캔들 상태·캔들 손절라인 행 미이식**(지시 — 가격 구조 섹션에서 제거). 판정 변수(추세 폭 변화 봉수·평탄 ATR 배수·크로스 주의 %·지지/저항 near %·σ 배수·vwap상태 전용 봉수)는 원본 명칭/기본값과 동일하며 **전략성 파라미터(수학자 리뷰 대상)** 입니다. 테이블은 `barstate.islast`에서만 그려 리페인팅과 무관합니다. |
| 윌리엄스 프랙탈(확정·게이트) | `확정 프랙탈 신호 표시` 옵션(기본 **ON**)이 켜져 있으면, **표준 윌리엄스 프랙탈**(중심봉 + 좌우 각 `fracN`봉, 기본 `fracN=2` → 총 5봉)을 판정해 **하방(저점) 프랙탈=매수 삼각형(파랑, 아래)·상방(고점) 프랙탈=매도 삼각형(빨강, 위)** 을 `offset −fracN`으로 중심봉에 소급 표시합니다(WILLIAMS-FRACTAL/`williams-fractal.pine` 이식). 확정은 중심봉 기준 `fracN`봉 지연이며, 한 번 확정된 프랙탈은 재계산되지 않고 미래참조도 없어 **리페인팅이 아닙니다**. `엄격 모드`(기본 ON)면 인접 봉 단조(피라미드/계곡)를 요구합니다. 같은 중심봉이 고점이자 저점이라 상방·하방이 **동시 확정**되면 중심봉 봉 색(`close[fracN]` vs `open[fracN]`)으로 1개만 남깁니다(양봉→매수·음봉→매도·도지→둘 다 억제). ⚠️ **각도필터(로직1 — ratio 대칭 게이트, 알고리즘 수학자 사양)**: 중심봉 기준으로 얼린 ATR(`atrRef = ATR(atrLen)[fracN]`)로 좌/우 기울기를 정규화해, **유효성**(둘 다 양수) + **강도**(`max(좌,우) ≥ slopeMin`, 미세 프랙탈 탈락) + **대칭비**(`min/max ≥ symMin`)를 모두 통과한 프랙탈만 삼각형으로 그립니다(`degree/atan` 미사용, ratio만). 고점(매도)=과거 상승·미래 하강, 저점(매수)=과거 하강·미래 상승으로 **양방향 대칭** 적용. 기본값 `symMin=0.5`·`slopeMin=0.5`·`atrLen=14`. **표시 전용**(단일 신호가 아니라 이치모쿠·테이블·보조지표 정렬 확인용). |
| 프랙탈 후보(1봉 조기, 삼각형/역삼각형) | `후보(1봉 조기) 마커 표시` 옵션(기본 **ON**)이 켜져 있으면, 확정 **1봉 전**에 **후보**를 **삼각형/역삼각형 마커**(`offset −1`)로 미리 표시합니다(로직2 — 알고리즘 수학자 사양). ⚠️ **후보 반투명 배경은 제거됐습니다(본부 지시)** — 이전에는 후보 감지 봉에 매수 후보=파랑·매도 후보=빨강 반투명 배경(`bgcolor`)과 **원 마커**를 그렸으나, **배경 `bgcolor` 2개(매수/매도)와 관련 input 3종(`매수 후보 배경색`·`매도 후보 배경색`·`후보 배경 투명도 fracCandBgTransp`)을 삭제**하고, 마커를 **삼각형/역삼각형으로 교체**했습니다. **마커 형태·색·투명도**: 매수 후보(하방/저점 프랙탈)=**삼각형 위(▲, `shape.triangleup`, `location.belowbar`)**, 매도 후보(상방/고점 프랙탈)=**역삼각형(▼, `shape.triangledown`, `location.abovebar`)**. 색은 **확정 프랙탈 색을 공유**(`매수(하방 프랙탈) 색`=파랑·`매도(상방 프랙탈) 색`=빨강)하되 **투명도 50**(`후보 마커 투명도 fracCandMarkerTransp`, 기본 **50**, 0~100)을 적용해 확정 삼각형(불투명)과 시각 구분합니다(`color.new(fracBuyColor, 50)`/`color.new(fracSellColor, 50)`). 별도 후보 색 input은 없습니다. 후보 시점: 중심봉=`[1]`·미래 1봉=`[0]`, `atrRefC = ATR(atrLen)[1]`로 **봉당 기울기**(과거 구간은 `/fracN`로 봉당 환산, 미래는 1봉 그대로)를 재계산해 유효성 + 강도(`≥ slopeMin`) + **완화된 대칭비**(`symMinCand = symMin × candRelax`, 기본 `candRelax=0.7` → `0.5×0.7=0.35`)를 통과하면 후보입니다(판정식·offset·전이 로직은 불변). **후보→확정/폐기 전이는 상태 저장(var 래치) 없이 순수 함수적**(매 봉 `[1]`/과거참조 재계산)입니다. ⚠️ **non-repainting 통일(본부 지시)**: 후보 삼각형/역삼각형 마커를 **확정봉(`barstate.isconfirmed`)에서만 표시**하도록 게이트를 걸어 실시간 봉 점멸을 없앴습니다. 후보는 여전히 확정(offset −fracN)보다 **1봉 빠르게**(중심봉+1 = offset −1) 뜹니다. 과거봉의 `[1]`/`[1+fracN]`/`[0]` 입력은 확정 후 불변이라 재평가돼도 동일 → 리페인팅이 없습니다. 상승 반전(저점 매수)·하락 반전(고점 매도) **양방향 대칭**. **표시 전용**. |
| 윌리엄스 %R 다이버전스 원 | `%R 다이버전스 원 표시` 옵션(기본 **ON**)이 켜져 있으면, **윌리엄스 %R**(`ta.wpr(wprLen)`, 기본 14, 종가 기준) 강세/약세 다이버전스 발생 봉에 **원 마커**(`offset 0`, 확정된 현재봉)를 표시합니다(`williams-fractal.pine` 이식). **강세(매수)=녹 원(belowbar)** = 직전 봉 %R가 하단 밴드(`wprLower`, 기본 −80) 아래로 진입한 상태에서 현재 봉 종가는 낮아졌는데 %R은 상승(`close<close[1] and wpr>wpr[1]`). **약세(매도)=노랑 원(abovebar)** = 직전 봉 %R가 상단 밴드(`wprUpper`, 기본 −20) 위로 진입한 상태에서 종가는 높아졌는데 %R은 하락(`close>close[1] and wpr<wpr[1]`). %R 라인 자체는 오버레이 부적합이라 그리지 않고 원 마커만 표시하며, `close[1]`·`wpr[1]`·`wpr` 모두 확정/과거참조라 **리페인팅이 없습니다**. 원은 프랙탈 삼각형보다 아래 레이어(먼저 호출)로 그려집니다. **표시 전용**. |

참고:
- 누적 방식은 **Recent Bars 롤링 단일 고정**이며(앵커모드 제거), 기본값은 `Recent Bars = 50`, `Bands Calculation Mode = Log Standard Deviation`입니다. 즉 기본 화면 상태는 **최근 50봉 기준 VWAP + 로그 표준편차 밴드**입니다.
- `Recent Bars`는 최근 리듬 기준선에 더 가깝습니다. 세션/거래일 기준 앵커는 더 이상 제공하지 않습니다.
- `Band #2` 바깥 자체가 바로 반전은 아니고, 강한 추세에서는 바깥 체류가 이어질 수 있습니다.

## 트레이딩 의미 핵심 4가지

### 1. 가격이 VWAP 위 / 아래에 있는 의미

가격이 VWAP 위에 있으면 `선택한 기준 구간 평균 단가보다 위에서 거래 중`, 아래에 있으면 `평균 단가보다 아래에서 거래 중`이라고 읽습니다.

- 방향 예측선이라기보다 현재 가격의 `비용대 기준 위치`를 보는 도구입니다.
- 실전에서는 `회복 후 유지`, `이탈 후 재진입 실패` 쪽이 더 중요합니다.

요약하면, VWAP은 `평균 단가 기준선`입니다.

### 2. VWAP 근처 왕복 의미

VWAP 근처를 계속 왕복하면 `방향성 추세`보다 `평균 단가 공방`이 진행 중일 가능성이 큽니다.

- 이 구간에서는 추격 진입보다 방향 확정 대기가 더 낫습니다.
- 다른 지표가 좋아 보여도 VWAP 공방이 길면 후속이 약할 수 있습니다.

요약하면, VWAP 공방은 `판단 유보 구간`으로 보는 편이 안전합니다.

### 3. Band #1 접근 의미

Band #1 접근은 `정상 확장` 또는 `첫 과열 구간`에 더 가깝습니다.

- 추세가 막 붙기 시작하는 구간에서도 자주 나옵니다.
- 따라서 Band #1 자체를 반전 신호로 보기보다 `확장 확인`으로 읽는 편이 좋습니다.

요약하면, Band #1은 `추세 확장 확인선`에 가깝습니다.

### 4. Band #2 접근 의미

Band #2 접근이나 이탈은 `강한 확장`, `과열 / 과매도`, `과확장 감시` 의미가 더 큽니다.

- 강한 추세에서는 바깥 체류가 이어질 수 있습니다.
- 그래서 Band #2는 바로 역방향 진입보다 `속도 조절`, `관리`, `후속 확인` 쪽에 더 유리합니다.

요약하면, Band #2는 `공격`보다 `과확장 감시`의 의미가 강합니다.

## 내부 로직

### 1. 기준 구간 — Recent Bars 롤링 단일 고정

VWAP 누적 방식은 **최근 N봉 롤링(Recent Bars) 하나로 고정**되어 있습니다(CEO 지시 — 앵커모드 제거). 과거의 `Session`/`Recent Trading Days` 앵커 분기와 세션 리셋(`ta.change(time("D"))` 류)·세션 누적 배열은 **완전히 삭제**됐습니다. 알고리즘 수학자가 지적한 DRIFT(원본 이치모쿠의 `vwap상태(20)`는 앵커 무관 Recent Bars 고정이 정답)를 해소하려는 조치이며, 화면 50봉·`vwap상태(20)`·중기추세 `vwapValue`·지지저항 등 **전 VWAP이 Recent Bars 단일로 계산**됩니다.

`Recent Bars`
- 최근 `N`봉(`Recent Bars`, 기본 50)을 하나의 이동 창처럼 계산합니다(앵커 = `N`봉 전 누적값).
- 세션 경계를 무시하고 차트에 보이는 현재 타임프레임 기준 봉 수로 바로 계산합니다.
- 워밍업(봉 수 < `N`)에는 창이 전체 누적으로 자연 폴백됩니다.

### 2. VWAP 계산

현재 코드는 아래 값을 누적해서 씁니다.

- `src * volume`
- `volume`
- `src^2 * volume`

이 누적값으로:
- `VWAP`
- 거래량 가중 분산
- 표준편차 기반 밴드

를 계산합니다.

`computeVwapSet(barsLen, srcArg)` 헬퍼가 window 길이(Recent Bars 롤링 length)와 **소스**를 파라미터로 받아 화면 세트(`length=50`, `src`)를 계산합니다. 누적값(`cumulative*`)은 **함수-로컬 `var`로 내부화**했습니다 — Pine v6에서 함수 내부 `var`는 호출부마다 독립 상태를 가지므로 확정봉 기준으로 누적하며 리페인팅이 없습니다. window = 현재 누적 − `barsLen`봉 전 누적(`cumulative*[barsLen]`)이라 항상 최근 `barsLen`봉 롤링입니다(앵커모드 제거로 세션/거래일 분기·세션 앵커 배열은 삭제됨). 밴드·밴드2도 이 세트의 window에서 산출됩니다. 산식(rolling VWAP + 로그/절대 표준편차 밴드)은 앵커모드 제거 전의 Recent Bars 경로와 동일합니다.

### 3. 밴드 계산 방식

`Bands Calculation Mode`는 두 가지입니다.

`Log Standard Deviation`
- 현재 기본값입니다.
- 거래량 가중 `log(price)` 표준편차를 사용합니다.
- 밴드는 `VWAP * exp(± sigma_log * multiplier)` 형태로 계산되어, 절대 가격보다 비율 변화에 더 민감합니다.
- 가격이 0 이하인 자산에는 적합하지 않습니다.

`Absolute Standard Deviation`
- 거래량 가중 표준편차를 절대 가격 단위로 계산해 밴드 폭으로 사용합니다.

즉 이 지표는 `VWAP 기준선 + 확장 폭 방식`을 바꿔가며 읽는 구조입니다.

> **상태 테이블(재도입):** 이전에는 상태 테이블을 두지 않았으나, SUPER-ICHIMOKU 테이블을 이식해 **우측 상단 상태 테이블을 재도입**했습니다(`상태 테이블 표시` 옵션, 기본 ON — 위 "상태 테이블" 행 참조). 3개 섹션(추세 확인 / 가격 구조 및 변동성 확인 / 지지·저항 확인)으로, 판정식은 이치모쿠 원본 로직 그대로입니다(캔들 상태·캔들 손절라인 행은 미이식). **반등 감시(`abnWatch`)** 는 이제 이 테이블의 `비정상 매수/매도` 행에 "롱 감시/숏 감시"로 표기됩니다(그간 보류했던 표시가 테이블 재도입으로 연결됨). **비정상 매수/매도**는 배경색 + 이 테이블 셀로 함께 표시하며, **이격도**도 래치 리셋 판정에 더해 테이블 `이격도 (20)` 셀로 표기합니다.

### 4. 비정상 매수/매도(과매수/과매도) — 이치모쿠 이식

SUPER-ICHIMOKU 메인 지표(`advanced-ichimoku.pine`)의 **"비정상 캔들"** 판정을 그대로 이식했습니다. 목적은 **평균 회귀가 필요한 극단 구간(과매수/과매도)** 을 배경색으로 한눈에 드러내는 것입니다.

- **cRSI(도미넌트 사이클 RSI)**: 사이클 길이 = `비정상 기준 길이 ÷ 2`(기본 20÷2=10)의 RSI를 Vibration(기본 10)으로 위상 보정·평활한 값. `≥ 절대 과매수(70)`면 과매수, `≤ 절대 과매도(30)`면 과매도.
- **볼린저 밴드 이탈**: `SMA(close, 비정상 기준 길이) ± stdev × 배수(2.0)`. **볼린저 길이는 비정상 전체 기준 길이(`비정상 기준 길이(볼린저 등)`, 기본 20)** 와 동일합니다(CEO 지시로 단일화 — cRSI·ATR·RSI·추적 lookback도 모두 이 값에 연동). 종가가 아닌 **고가/저가**로 판정합니다(종가 실시간 변동이 커서). 상단 초과폭(또는 하단 이탈폭) ÷ 캔들 범위가 `비정상 밴드 초과 비율(0.3)` 이상이면 이탈로 인정.
- **비정상 판정(AND)**: 비정상 매수(=과매수) = cRSI 과매수 **AND** 고가 상단 이탈, 비정상 매도(=과매도) = cRSI 과매도 **AND** 저가 하단 이탈.
- **상태 래치·리셋**: 비정상 봉이 나오면 그 방향 상태를 래치해 다음 비정상 봉까지 유지하고, **이격도(Disparity)가 중립으로 복귀하면 리셋**합니다. 이격도 = `종가 ÷ 이평(기본 SMA 20) × 100`으로, 고정 임계(±10%)와 적응형(자기 표준편차 σ×1.5) 두 기준의 충족 개수로 되돌림을 판정하며, **둘 다 미충족(중립)이면 리셋 트리거**입니다.
- **반등 감시(abnWatch) — 이치모쿠 반등 감시 이식(CEO 지시)**: 비정상 캔들(극단) 발생 뒤 **그 방향과 반대되는 되돌림(반등)** 이 유의미하면 **`abnWatch` 상태**(`+2`=롱 감시, `−2`=숏 감시, `0`=없음)로 래치합니다. 판정 = 비정상 캔들이 뜬 봉의 극단가(롱=`low`·숏=`high`)·RSI·ATR을 기준점(`Ref*`)으로 스냅샷한 뒤, **`abnLookback`(=`비정상 기준 길이`) 봉 이내**에서 ① 반대방향 가격 되돌림(`priceMove`, 롱=`RefPrice−low`·숏=`high−RefPrice`) ≥ `RefAtr × 비정상 반등 감시 ATR 배수`(기본 **1.5**) **AND** ② RSI 되돌림 폭(`rsiMove`) ≤ `비정상 RSI 이동 제한`(기본 **15**)이면 감시 ON. RSI·ATR은 이치모쿠 원본과 동일 길이(`ta.rsi(close, abnLen)`·`ta.atr(abnLen)`, `abnLen=비정상 기준 길이`)이며, 이치모쿠의 **기본/강화 2단 감시는 단일 감시로 통합**된 최신본을 이식했습니다. 리셋은 비정상 상태와 함께 **이격도 중립 복귀 시** `abnWatch`도 `0`으로 돌립니다. ⚠️ **리페인팅 방지(CEO 명시)**: 이치모쿠 원본은 실시간 '빠른 캐치'를 위해 확정봉 가드를 의도적으로 안 걸었으나, 본 이식은 CEO 지시대로 **기준점·감시 래치·리셋을 모두 확정봉(`barstate.isconfirmed`)에서만 갱신**합니다(형성 중 현재봉이 래치를 흔들지 않음 → 신규 리페인팅 없음). 이 실시간성 차이는 CEO 보고 대상입니다. ⚠️ **화면 표시 보류(CEO 판단 대기)**: auto-vwap엔 상태 테이블/셀이 없어(아래 "상태판 없음") 이치모쿠처럼 셀 텍스트("롱 감시"/"숏 감시")로 표기할 자리가 없습니다. 억지로 새 UI를 만들지 않고 **로직만 이식(abnWatch 계산·래치까지)** 했으며, 표시 방식(마커/라벨/셀 등) 확정은 CEO 판단을 기다립니다. `abnWatch` 상태는 이미 계산·래치되어 있어 표시 방식 확정 시 즉시 연결 가능합니다. `비정상 반등 감시 ATR 배수`·`비정상 RSI 이동 제한`은 전략성 파라미터(수학자 리뷰 대상)입니다.

⚠️ **자기완결 이식 근거**: 원본은 기준 길이를 이치모쿠 베이스 라인(`basePeriods`)에, 리셋을 이치모쿠 이격도 블록에 의존했습니다. auto-vwap엔 그 둘이 없으므로 (1) 기준 길이를 **단일 input(`비정상 기준 길이(볼린저 등)`, 기본 20)** 하나로 통일하고(CEO 지시 — 볼린저 + cRSI·ATR·RSI·추적 lookback이 모두 이 값에 연동. 구 별도 `비정상 기준 길이(베이스 라인 대응)` 기본 22는 삭제·통합해 전 기준 길이 22→20으로 통일), (2) 이격도를 **원본 산식·input 그대로 이식**해 자기완결화했습니다. 소스가 같은 기본값이면 원본과 동일한 값을 재현합니다. 반등 감시(`abnWatch`)도 원본 산식(기준점·되돌림 게이트·부호 체계 ±2)을 그대로 이식했고, 두 신설 input(`비정상 반등 감시 ATR 배수`·`비정상 RSI 이동 제한`)은 **title을 이치모쿠와 일치**시키되 auto-vwap의 UI 일관성을 위해 기존 `비정상 매수/매도` 그룹에 배치했습니다(원본 그룹명은 `비정상 캔들`). 매매 신호가 아니라 **표시 전용 국면 표식**이며, 임계·기간은 전략성 파라미터라 임의 변경 시 수학자 리뷰 대상입니다.

## 지수 추세 리본(EMA/DEMA/TEMA) + 정배열/역배열 배경

CEO 설계로 추가한 **지수형 추세 리본**과 그 리본으로 판정하는 **정배열/역배열 배경**입니다. 매매 신호가 아니라 **표시 전용 국면 표식**이며, 기존 auto-vwap 기능(VWAP·밴드·SAR·비정상)은 전부 불변입니다.

### 1. 리본 3선(EMA/DEMA/TEMA)

- **계산(종가 기준, 화면 표시 대상).** 단일 소스 `close`에 EMA를 3중 누적합니다: `e1 = EMA(close, 지수 리본 길이)`, `e2 = EMA(e1, 지수 리본 길이)`, `e3 = EMA(e2, 지수 리본 길이)`. 여기서 **`EMA = e1`, `DEMA = 2·e1 − e2`, `TEMA = 3·e1 − 3·e2 + e3`**를 파생합니다. `지수 리본 길이` 기본 **20**. **화면에 그려지는 것은 이 종가 기준 3선뿐**입니다.
- **의미.** DEMA는 EMA의, TEMA는 DEMA의 지연을 각각 줄인 지수형 추세선입니다. 상승 추세가 붙으면 **TEMA > DEMA > EMA(정배열)**, 하락 추세면 역배열로 벌어지며, 세 선의 상대 위치·기울기가 추세 국면을 압축해 보여줍니다.
- **표시.** 각 선 **1px**, 색은 옵션(`EMA 선 색`=노랑 / `DEMA 선 색`=하늘색 / `TEMA 선 색`=분홍). **우측 가격축 라벨 없음**(`display.all − display.price_scale` — 차트 선만, 가격축 숫자 라벨 끔). `지수 추세 리본 표시(EMA/DEMA/TEMA)` 토글(기본 **ON**).
- **리페인팅 없음.** `ta.ema` 누적(확정 성격)이라 과거봉 값이 확정되어 신규 리페인팅이 없습니다.

### 2. 정배열/역배열 배경 (저가/고가 기준 2세트 리본, 선 기울기 크기 순서, CEO 재설계)

**배경 판정 전용 리본 2세트**(계산만, 화면 표시 없음)의 **직전봉 대비 기울기 크기 순서**로 정배열/역배열을 판정하고, 그 봉의 밴드2(상단2~하단2) 영역을 배경색으로 채웁니다. 위 화면 표시 3선(종가 기준)과 **별개**이며, 녹은 저가 기준·빨강은 고가 기준 리본으로 판정합니다(CEO 지시). 이전의 갭·ATR 정규화·게이트·streak 방식은 **전부 폐기**하고 아래 단순 부등식으로 재작성했습니다.

- **배경 판정 전용 리본(plot 없음).** 종가 리본과 동일 산식, 소스만 다릅니다.
  - **저가 기준(녹 판정용):** `emaLow = EMA(low, len)`, `demaLow = 2·emaLow − EMA(emaLow)`, `temaLow = 3·emaLow − 3·EMA(emaLow) + EMA(EMA(emaLow))`.
  - **고가 기준(빨강 판정용):** `high` 소스로 동일 → `emaHigh` / `demaHigh` / `temaHigh`.
  - 이 2세트는 **화면에 그리지 않습니다**(plot 개수 불변). 배경색 결정에만 씁니다.
- **기울기(단순 차분, 가격 단위).** 저가 리본 `sEmaLow`/`sDemaLow`/`sTemaLow`, 고가 리본 `sEmaHigh`/`sDemaHigh`/`sTemaHigh`. 세 선이 **같은 가격축·같은 단위(slope)**라 크기를 그대로 순서 비교할 수 있어 **ATR 정규화가 필요 없습니다**(스케일 상쇄).
- **판정.**
  - **녹(정배열)** = 저가 기준 리본 `sTemaLow > sDemaLow and sDemaLow > sEmaLow` — 기울기 **크기 순서만**(부호 무관). 예: +10 > +5 > −10 → **녹**.
  - **빨강(역배열)** = 고가 기준 리본 `sTemaHigh < sDemaHigh and sDemaHigh < sEmaHigh` — 크기 순서만(부호 무관). 반대 패턴.
  - **각도 체크 순서 로직은 종전과 불변**(부호 조건 없는 순서 비교 그대로)이며, 소스만 녹=저가/빨강=고가로 분리했습니다.
  - **동시 발생 처리(CEO — 봉 색 + 꼬리 거부).** 녹·빨강이 **서로 다른 리본**(저가 vs 고가)이라 **동시에 성립할 수 있습니다**. 둘 다 true면 **확정봉 봉 색과 꼬리**로 배경을 결정합니다. 봉 색이 방향을 주되, **몸통보다 긴 반대 꼬리(상방/하방 거부)가 나오면 색을 뒤집습니다** — **양봉(`c1>o1`): 윗꼬리 ≤ 몸통 → 녹(1) / 윗꼬리 > 몸통 → 빨강(−1, 상방 거부)**, **음봉(`c1<o1`): 아래꼬리 ≤ 몸통 → 빨강(−1) / 아래꼬리 > 몸통 → 녹(1, 하방 거부)**, **도지(`c1==o1`) → 중립(0·배경 없음)**. `body1 = |c1−o1|`, `upWick1 = h1 − max(c1,o1)`, `dnWick1 = min(c1,o1) − l1`(모두 확정봉 `[1]`: `o1/c1/h1/l1`). "길면"=초과(`>`)·"이하"=`<=`이며, 도지는 `greenPick`·`redPick` 둘 다 false라 0:
    ```
    o1 = open[1]
    c1 = close[1]
    h1 = high[1]
    l1 = low[1]
    body1 = math.abs(c1 - o1)
    upWick1 = h1 - math.max(c1, o1)
    dnWick1 = math.min(c1, o1) - l1
    greenPick = (c1 > o1 and upWick1 <= body1) or (c1 < o1 and dnWick1 > body1)
    redPick = (c1 < o1 and dnWick1 <= body1) or (c1 > o1 and upWick1 > body1)
    ribbonState =
        greenRaw and redRaw ? (greenPick ? 1 : redPick ? -1 : 0)
        : greenRaw ? 1
        : redRaw   ? -1
        : 0
    ```
    **봉 색·꼬리·몸통 모두 배경 판정과 동일한 확정봉 `[1]` 기준**입니다 — 배경 기울기 판정이 `[1] vs [2]` 확정봉 기준이므로 색·꼬리도 확정봉으로 일관시켜 형성 중 현재봉 변동에 따른 배경 점멸(리페인팅)이 없습니다. **도지는 봉 색이 방향을 주지 못하므로 중립(배경 없음)** 처리합니다. **꼬리 거부의 의미**: 몸통보다 긴 반대 꼬리는 그 봉이 밀린 방향(양봉인데 위꼬리↑ = 상방 거부, 음봉인데 아래꼬리↓ = 하방 거부)을 뜻해 봉 색과 반대의 국면색으로 뒤집습니다. `ribbonState = 1(정·녹) / −1(역·빨강) / 0(중립 → 배경 `na`)`.
- **밴드1 이탈 + 긴 꼬리 억제 게이트(CEO 지시).** 위 `ribbonState`가 확정된 뒤, 특정 봉을 **밴드1 이탈 + 긴 꼬리** 조건으로 **중립(0·배경 미표시)** 처리합니다. **녹(정배열, `ribbonState==1`) 억제** = 확정봉 고가가 밴드1 상단 위(`high[1] > upperBandValue1[1]`) **AND** 윗꼬리 > 몸통(`upWick1 > body1`) → 밴드1 위로 튀어나간 봉이 긴 윗꼬리(상방 거부)를 달면 정배열 배경을 미표시. **빨강(역배열, `ribbonState==−1`) 억제** = 확정봉 저가가 밴드1 하단 아래(`low[1] < lowerBandValue1[1]`) **AND** 아래꼬리 > 몸통(`dnWick1 > body1`) → 밴드1 아래로 이탈한 봉이 긴 아래꼬리(하방 거부)를 달면 역배열 배경을 미표시. 억제는 **녹/빨강 전체에 적용**(동시 발생뿐 아니라 단독 케이스 포함)합니다: `greenSuppress = high[1] > upperBandValue1[1] and upWick1 > body1`, `redSuppress = low[1] < lowerBandValue1[1] and dnWick1 > body1`, `ribbonState := (ribbonState==1 and greenSuppress) or (ribbonState==−1 and redSuppress) ? 0 : ribbonState`. **기준·리페인팅**: `high[1]`/`low[1]`·밴드1`[1]`(`upperBandValue1[1]`/`lowerBandValue1[1]`)·`upWick1`/`dnWick1`/`body1` 모두 배경 판정과 동일한 확정봉 `[1]` 세트값이라 신규 리페인팅이 없습니다. **`na` 가드**: 밴드1`[1]`·`high[1]`(녹)/`low[1]`(빨강)이 `na`(워밍업)면 억제식이 false → 억제하지 않습니다(안전 기본, 억제는 배경을 빼는 방향으로만 작동). "길면"=초과(`>`), 밴드1 이탈="위/아래"(고가/저가 vs 밴드1). `greenRaw`/`redRaw`·각도 순서·동시 처리(봉색+꼬리)·밴드2 fill·색·토글은 **불변**이며 억제 게이트만 추가된 것입니다(가격 항만 `close[1]`→`high[1]`(녹)/`low[1]`(빨강)로 정정).
- **리페인팅 처리(CEO 권장 = 확정봉 기준).** 세 리본(종가/저가/고가) 모두 기울기를 **직전 확정봉 기준**으로 계산합니다: 예 `sTemaLow = temaLow[1] − temaLow[2]`, `sTemaHigh = temaHigh[1] − temaHigh[2]`. 직전봉 대비 차분을 그대로(현재봉 값으로) 쓰면 형성 중 미마감봉에서 현재봉 line 값이 변동해 배경이 **점멸**할 수 있으므로, 한 봉 밀어 확정된 봉의 기울기로 판정합니다. 저가/고가 리본도 `ta.ema` 누적·`[1]`/`[2]` 과거참조라 과거봉 값이 불변 → **신규 리페인팅 없음**, 배경은 확정봉 기준으로 **한 봉 지연** 반영됩니다. 워밍업 봉에서 `[2]`가 `na`면 차분이 `na`가 되어 부등식이 false로 떨어져 배경이 뜨지 않습니다(`slopeValidLow`/`slopeValidHigh` 가드).
- **배경(밴드2 영역 한정 fill).** `upperBand_2`/`lowerBand_2` 두 선 사이 영역을 `정배열=녹(#2E7D32)`/`역배열=빨강(#C62828)` 반투명(기본 투명도 **80**)으로 채웁니다. **밴드2 선(plot) 색은 불변**이고, `shouldPlot`·밴드2 `na` 가드가 워밍업을 방어합니다.
- **표시 전용.** 매매 판정·밴드와 무관합니다. `지수 리본 길이`(기본 20)만 전략성 파라미터(수학자 리뷰 대상)이며, 배경 판정 자체엔 별도 임계 입력이 없습니다(기울기 크기 순서만 봄).

## 레이어(z-order) 순서 — 실제 순서(auto-scale 폭발 버그 수정 반영)

차트에 겹쳐 그려지는 페인팅의 **앞뒤(z-order)** 순서입니다. Pine v6의 렌더 순서는 `explicit_plot_zorder=true`(스크립트 상단 `indicator()`에 지정)에서 **`plot()`/`hline()`/`fill()`의 코드 선언 순서**로 결정됩니다(먼저 선언 = 더 뒤, 나중 선언 = 더 앞). **단 `bgcolor()`는 이 인자와 무관하게 항상 최후면 그룹**입니다. 원래 CEO 지시는 "뒤 VWMA → 중간 비정상 배경 → 앞 나머지"의 3계층이었고 이를 위해 비정상 배경을 `bgcolor`에서 plot 기반 fill로 전환했으나, **그 극단값(`±1e20`) fill이 auto-scale을 폭발시키는 심각한 버그가 실측 확인**되어(위 "비정상 매수/매도 배경" 행 참조) **비정상 배경을 `bgcolor`로 복원**했습니다. `bgcolor`가 항상 최후면이므로 **실제 순서는 아래와 같습니다(뒤 → 앞)**:

1. **가장 뒤(back-most): 비정상 매수/매도 배경(`bgcolor`).** `explicit_plot_zorder`로 순서 제어가 안 되며 모든 `plot`/`fill`보다 뒤에 그려집니다. auto-scale 폭발을 막기 위한 유일한 화면 전체 배경 렌더 수단입니다(극단값 fill 제거). 색·조건·투명도·판정(`abnBg`/`abnShowBg`/`abnCandleState`)은 불변.
2. **중간: 거래량 가중선 50/100/200 + 100~200 fill.** `plot`/`fill` 중에서는 가장 먼저 선언되어 나머지 plot/fill보다 뒤입니다(비정상 배경 bgcolor보다는 앞).
3. **가장 앞(front-most): 나머지 모든 페인팅.** VWAP 본선·가격축 라벨, 밴드1/밴드2 + 각 fill, 정배열/역배열 배경(밴드2 영역 fill), 지수 리본(EMA/DEMA/TEMA), 파라볼릭 SAR, 그리고 프랙탈 삼각형/후보 마커·%R 다이버전스 원(`plotshape`)입니다. `plotshape`와 드로잉(라벨·상태 테이블)은 `explicit_plot_zorder` 영향 밖에서 각자 상위 z-그룹의 최전면을 유지하므로 자동으로 "가장 앞"에 포함됩니다.

⚠️ 이 순서는 **색·조건·투명도·판정·값·산식을 일절 바꾸지 않고 렌더 방식(비정상 배경 극단값 `fill`→`bgcolor` 복원)만** 조정한 결과입니다. 비정상 배경이 VWMA선 뒤로 가는 것(CEO가 원한 "중간"이 아니라 "가장 뒤")은 Pine 구조상 auto-scale과 양립하기 위해 불가피합니다. **최종 표시·auto-scale 정상화는 TradingView 컴파일·실차트에서 재확인이 필요합니다.**

## 윌리엄스 프랙탈 + 각도필터(ratio) + 후보(삼각형/역삼각형) + %R 다이버전스

WILLIAMS-FRACTAL 지표(`williams-fractal.pine`)의 **프랙탈 판정·엄격 모드·%R 다이버전스** 로직·페인팅을 이식하고, 알고리즘 수학자가 설계한 **각도 정교화 2종(로직1 ratio 게이트 + 로직2 후보 마커)**을 신규 구현한 블록입니다. ⚠️ **후보 반투명 배경은 제거됐습니다(본부 지시)** — 후보는 이제 삼각형/역삼각형 마커로만 표시합니다(아래 로직2 참조). 매매 신호가 아니라 **표시 전용 국면 표식**이며, 단일 신호가 아니라 이치모쿠·테이블·보조지표 정렬 확인용으로 설계했습니다(펀드 철학: 단일 신호 불신). 기존 auto-vwap 기능(VWAP·밴드·SAR·비정상·리본·테이블)은 전부 불변입니다.

### 1. 확정 프랙탈(양방향, williams-fractal 원본 이식)

- **판정(표준 5봉).** 중심봉 + 좌우 각 `fracN`봉(기본 2 → 총 5봉). **하방(저점) 프랙탈**=중심봉 `low[fracN]`이 좌우 각 봉 저가보다 모두 낮음 → **매수**. **상방(고점) 프랙탈**=중심봉 `high[fracN]`이 최고 → **매도**. 좌측(과거)=`[fracN+i]`·우측(최근)=`[fracN-i]`(i=1..fracN). 함수 `isDownFractal(len)`/`isUpFractal(len)`은 원본과 동일합니다.
- **엄격 모드**(`fracStrictMode`, 기본 **ON**). 표준식을 **인접 봉 단조**(계단식 산/계곡)로 강화합니다 — 상방 좌측 단조상승·우측 단조하강(피라미드), 하방은 부등호 반전(계곡). n=2면 `high[len+2]<high[len+1]<high[len]>high[len-1]>high[len-2]`. OFF면 중심이 좌우보다 높기/낮기만.
- **동시 확정 처리.** 같은 중심봉이 국소 고점이자 저점이면 상방·하방이 동시 확정될 수 있어(드묾), 중심봉 봉 색(`close[fracN]` vs `open[fracN]`)으로 1개만 남깁니다 — **양봉→매수만·음봉→매도만·도지→둘 다 억제**(중립·자본 보존, 동시 확정일 때만 발동).
- **표시·리페인팅.** 확정 삼각형은 `offset −fracN`으로 중심봉에 소급합니다(중심봉 + 우측 `fracN`봉이 모두 나온 뒤 확정 → `fracN`봉 후행). 한 번 확정된 프랙탈은 재계산되지 않고 미래참조(`request.security` lookahead 등)도 없어 **소급 표시는 리페인팅이 아닙니다**.

### 2. 로직1 — 가짜 프랙탈 제거(ratio 대칭 게이트, 알고리즘 수학자 사양)

확정 프랙탈 중 **좌우 비대칭이거나 미세한** "가짜 프랙탈"을 ratio 게이트로 걸러, 게이트를 통과한 프랙탈만 삼각형으로 그립니다. **판정은 대칭비(ratio)만** 쓰고 `degree/atan`은 쓰지 않습니다.

- **정규화 척도.** 프랙탈 전용 `fracAtrN = ta.atr(atrLen)`(`atrLen` 기본 **14**)을 **중심봉 기준으로 얼려** 사용: `atrRef = fracAtrN[fracN]`. (기존 auto-vwap의 `abnAtr`/`ltAtrTable`와 별개 — 변수 접두어 `frac*`로 충돌 회피.)
- **기울기(ATR 정규화).**
  - 고점(매도): `slopeUpRise = (high[fracN] − high[fracN+fracN]) / atrRef`(과거 상승), `slopeUpDrop = (high[fracN] − high[fracN−fracN]) / atrRef`(미래 하강).
  - 저점(매수, 부호 반전): `slopeDnFall = (low[fracN+fracN] − low[fracN]) / atrRef`(과거 하강, 양수), `slopeDnRise = (low[fracN−fracN] − low[fracN]) / atrRef`(미래 상승, 양수).
- **게이트(양방향 대칭).** `유효 = 둘 다 > 0`, `강도 = max(좌,우) ≥ slopeMin`, `대칭비 = min(좌,우)/max(좌,우) ≥ symMin`. 세 조건 AND. `upFractalConfirmedGated = upFractalConfirmed and upGate`, `downFractalConfirmedGated = downFractalConfirmed and dnGate`. 삼각형 plotshape 조건을 이 `*Gated` 값으로 교체했습니다.
- **미세 프랙탈 탈락(CEO 확정).** `slopeMin` 게이트를 **활성**(기본 **0.5**)해 강도가 약한 미세 프랙탈을 탈락시킵니다. `symMin` 기본 **0.5**(범위 0.2~0.9, step 0.05). `atrRef`가 `na`(워밍업)이면 기울기가 `na`가 되어 게이트가 false → 삼각형 미표시(안전).

### 3. 로직2 — 후보(1봉 조기, 삼각형/역삼각형 마커)(알고리즘 수학자 사양 + 본부 지시)

확정보다 **1봉 먼저** 잠정 후보를 잡아 **삼각형/역삼각형 마커**로 예고합니다. ⚠️ **후보 반투명 배경은 제거됐습니다(본부 지시)** — 이전의 원 마커 + `bgcolor` 배경 대신 삼각형/역삼각형 마커만 씁니다.

- **후보 시점·봉당 기울기.** 중심봉=`[1]`·미래 1봉=`[0]`, `atrRefC = fracAtrN[1]`. 과거 구간은 **봉당** 기울기로 환산(`/fracN`), 미래는 1봉 그대로.
  - 고점(매도): `slopeUpRiseC = (high[1] − high[1+fracN]) / atrRefC / fracN`, `slopeUpDrop1 = (high[1] − high[0]) / atrRefC`.
  - 저점(매수, 대칭): `slopeDnFallC = (low[1+fracN] − low[1]) / atrRefC / fracN`, `slopeDnRise1 = (low[0] − low[1]) / atrRefC`.
- **후보 게이트.** 유효(둘 다 > 0) + 강도(`max ≥ slopeMin`) + **완화 대칭비**(`min/max ≥ symMinCand`), `symMinCand = symMin × candRelax`. `candRelax` 기본 **0.7**(확정 0.5 → 후보 **0.35**). 판정식·offset·전이 로직은 **불변**입니다.
- **순수 함수적 전이(리페인팅 최소).** 후보→확정/폐기 전이는 **상태 저장(var 래치) 없이** 매 봉 `[1]`/과거참조로 재계산합니다.
- **마커(삼각형/역삼각형, 배경 제거).** **매수 후보(하방/저점 프랙탈)=삼각형 위**(▲, `shape.triangleup`, `location.belowbar`), **매도 후보(상방/고점 프랙탈)=역삼각형**(▼, `shape.triangledown`, `location.abovebar`), 모두 `offset −1`(중심봉+1). 색은 **확정 프랙탈 색을 공유**(매수=파랑·매도=빨강)하되 **투명도 50**(`후보 마커 투명도 fracCandMarkerTransp`, 기본 **50**)을 적용해 확정 삼각형(`offset −fracN`, 불투명)과 시각 구분합니다(`color.new(fracBuyColor, 50)`/`color.new(fracSellColor, 50)`). 별도 후보 색 input은 없습니다.
- **⚠️ non-repainting 통일(본부 지시).** 후보 삼각형/역삼각형 마커를 `barstate.isconfirmed` 가드(`fracCandBuyShow = fracShowCandidate and dnCandidate and barstate.isconfirmed`, 매도는 `upCandidate`)로 **확정봉에서만 표시**해 실시간 봉 점멸을 없앴습니다. 후보는 여전히 확정(offset −fracN)보다 **1봉 빠르게**(중심봉+1 = offset −1) 뜹니다. 과거봉의 `[1]`/`[1+fracN]`/`[0]` 입력은 확정 후 불변이라 재평가돼도 동일 → 리페인팅이 없습니다.
- **양방향 대칭.** 상승 반전(저점 매수)·하락 반전(고점 매도) 모두 대칭 적용.

### 4. 윌리엄스 %R 다이버전스(williams-fractal 원본 이식)

- **%R.** `wpr = ta.wpr(wprLen)`(기본 14, 종가 기준, 범위 −100~0). 오버레이 부적합이라 **라인은 그리지 않고** 다이버전스 봉에 **원 마커**(`offset 0`, 확정된 현재봉)만 표시합니다.
- **강세(매수)=녹 원(belowbar).** `wpr[1] < wprLower`(직전 봉 하단 밴드 진입, 기본 −80) AND (`close < close[1]` AND `wpr > wpr[1]`) — 과매도 진입 후 종가↓·%R↑.
- **약세(매도)=노랑 원(abovebar).** `wpr[1] > wprUpper`(직전 봉 상단 밴드 진입, 기본 −20) AND (`close > close[1]` AND `wpr < wpr[1]`) — 과매수 진입 후 종가↑·%R↓.
- **레이어·리페인팅.** 원은 프랙탈 삼각형보다 **먼저** 호출해 아래 레이어(뒤)로 그려지고, 겹치면 삼각형이 앞에 보입니다. `close[1]`·`wpr[1]`·`wpr` 모두 확정/과거참조라 **리페인팅이 없습니다**.

⚠️ **컴파일 확인 필요.** 로컬 Pine 컴파일러가 없어 정적 리뷰(타입·`na`·`max_bars_back`·plot 개수 상한)로 검증했으며, 최종 동작은 **TradingView 컴파일·차트 확인**이 필요합니다.

## 자주 만지는 설정

| 설정 | 언제 조정하나 |
| --- | --- |
| `Recent Bars (기본 화면 VWAP)` | 화면에 그려지는 VWAP의 롤링 window 길이. 기본 50. (누적 방식은 Recent Bars 단일 고정 — 앵커모드 제거됨) |
| `Source` | 기준 가격을 `hlc3`, `close` 등으로 바꾸고 싶을 때 |
| `Bands Calculation Mode` | 로그 기준 밴드와 절대가격 표준편차 밴드를 바꾸고 싶을 때 |
| `Bands Multiplier #1`, `#2` | 확장 폭을 더 넓게 또는 좁게 보고 싶을 때 |
| `VWAP 선 지지/저항 색(구간 종가·꼬리 걸침 이탈)` | 화면 VWAP 중앙선 색을 봉 단위 지지=녹/저항=빨강/중립=흰색으로 칠할지. 기본 **ON**. 판정 구간은 **VWAP~밴드1의 동적 near 범위**(`upperNear`/`lowerNear`, 아래 `근처 구간 %` 참고)이며, **저항(빨강)=①`lowerNear≤close≤vwap` 또는 ②`close<lowerNear` AND `high>lowerNear`(꼬리 걸침); 지지(녹색)=③`vwap≤close≤upperNear` 또는 ④`close>upperNear` AND `low<upperNear`(꼬리 걸침); 그 외(완전 이탈·워밍업)=중립(흰색)** 입니다(CEO 확정 규칙, `close==vwap`은 저항 우선). ON이면 이 규칙이 VWAP 선 색을 담당(아래 `Enable Dynamic Line Color`는 VWAP 선엔 미적용) |
| `VWAP 근처 구간 최소 %(넓을 때)` / `최대 %(좁을 때)` | VWAP 선 지지/저항 판정의 near 구간 비율(`pct`)을 밴드폭에 **반비례**로 정합니다. 밴드폭이 넓을 때(고변동성)는 최소 %(기본 **20**), 좁을 때(저변동성)는 최대 %(기본 **60**)로 수렴합니다. `pct` × (VWAP~밴드1 거리)가 near 구간의 상/하한(`upperNear`/`lowerNear`)입니다 |
| `근처 % 저변동성 임계` / `고변동성 임계` (밴드1폭/VWAP, %) | `pct` 정규화 임계. 밴드1 폭 ÷ VWAP 비율이 저변동성 임계(기본 **0.5%**) 이하이면 최대 %, 고변동성 임계(기본 **2.0%**) 이상이면 최소 %, 사이는 선형보간(반비례)합니다. 저변동성 임계 < 고변동성 임계여야 하며, 뒤집히거나 `vwap==0`이면 최대 %로 폴백합니다 |
| `Enable Dynamic Line Color` | Band #2 이탈 시 선 색상 자동 변경(Band #1). VWAP 선엔 위 지지/저항 색이 OFF일 때만 적용 |
| `지수 추세 리본 표시(EMA/DEMA/TEMA)` | close 기준 EMA·DEMA·TEMA 3선(각 1px)을 그릴지. **기본 ON**. 표시 전용 오버레이(가격축 라벨 없음) |
| `지수 리본 길이` | EMA/DEMA/TEMA 공통 길이(3중 EMA 누적). 기본 **20**. 전략성 파라미터(수학자 리뷰 대상) |
| `EMA 선 색` / `DEMA 선 색` / `TEMA 선 색` | 리본 3선 색. 기본 **노랑 / 하늘색 / 분홍**. 표시 옵션 |
| `정배열/역배열 배경 표시` | 배경 판정 전용 리본(계산만·plot 없음)의 직전봉 대비 기울기 크기 순서로 정배열(녹=저가 기준 리본)/역배열(빨강=고가 기준 리본)을 판정해 밴드2 영역 배경으로 칠할지. 동시 성립 시 확정봉 봉 색 + 꼬리(양봉 윗꼬리≤몸통 녹·윗꼬리>몸통 빨강[상방 거부] / 음봉 아래꼬리≤몸통 빨강·아래꼬리>몸통 녹[하방 거부] / 도지 중립·배경 없음). **기본 ON**. 확정봉 기준 판정. 표시 전용 |
| `정배열 배경색` / `역배열 배경색` / `배경 투명도` | 정배열/역배열 배경 fill 색(기본 녹 #2E7D32 / 빨강 #C62828)과 투명도(기본 **80**). 표시 옵션이 켜졌을 때만 유효(`active`) |
| `VWAP·밴드 간격 % 라벨 표시` | VWAP/밴드 라인 끝에 붙는 **VWAP 대비 % 간격 라벨**(예 `+1.00%`)을 켜고 끌 때. **기본 꺼짐** |
| `거래량 가중 50선` / `거래량 가중 50선 색` | 거래량 가중 50선(VWMA 50, 2px)을 그릴지·색. **기본 ON**, 색 기본 **딥 오렌지 `#FF6D00`**(리본 노랑과 구분). |
| `장기선 100/200 표시` / `장기선 MA 종류(100/200)` / `100선 색` / `200선 색` / `100~200선 fill 투명도` | 거래량 가중 100·200선(각 2px)과 그 사이 fill. **기본 ON**, MA 종류 기본 **VWMA**, 색 기본 파랑·보라, fill 투명도 기본 75. |
| `장기선 MA #1/#2 길이(100/200)` | 100선·200선 길이. 기본 **100·200**. 전략성 파라미터(수학자 리뷰 대상). |
| `상태 테이블 표시(우측 상단)` | 우측 상단 상태 테이블(추세 확인 / 가격 구조 및 변동성 확인 / 지지·저항 확인)을 켤지. **기본 ON**. |
| `추세 폭 변화 판정 봉수` / `평탄 판정 ATR 배수` / `크로스 주의 간격(%)` | 단기/중기/장기 추세 셀의 강약·평탄·크로스 임박 판정 파라미터. 기본 **5 / 0.25 / 4.0**. 원본 동일. 전략성 파라미터. |
| `지지/저항 근처 구간 최소/최대 %` / `근처 % 저·고변동성 임계` / `100·200선 지지/저항 near 밴드 σ 배수` | 50/100/200선 지지·저항 + vwap상태(20) 판정의 동적 near 구간·밴드 파라미터. 기본 **20/60/0.5%/2.0%/1.0**. 원본 동일. |
| `vwap 상태 전용 최근 봉 수(6상태)` | vwap 상태(20) 6상태 판정 전용 별도 VWAP·밴드 윈도우. 기본 **20**. 화면 VWAP(50)과 이원화. 전략성 파라미터. |
| `좌우 봉 수 n (총 프랙탈 봉수 = 2n+1)` | 윌리엄스 프랙탈 폭(`fracN`). 기본 **2**(총 5봉). 확정은 `fracN`봉 지연(offset −n 소급). 로직1/로직2 기울기 참조 범위도 이 값에 연동. 전략성 파라미터(수학자 리뷰 대상). |
| `엄격 모드(피라미드/계곡 단조)` | 프랙탈 판정을 인접 봉 단조(피라미드/계곡)로 강화할지. **기본 ON**. OFF면 표준(중심이 좌우보다 높기/낮기만). |
| `확정 프랙탈 신호 표시` | 게이트(로직1) 통과 확정 매수(파랑 삼각형·아래)/매도(빨강 삼각형·위) 마커 표시. **기본 ON**. 표시 전용. |
| `대칭비 최소 symMin(확정)` | 로직1 ratio 게이트의 좌/우 기울기 대칭비 하한(`min/max`). 기본 **0.5**(범위 0.2~0.9, step 0.05). 1에 가까울수록 대칭만 인정. 전략성 파라미터(수학자 리뷰 대상). |
| `기울기 강도 최소 slopeMin(ATR 배수)` | 로직1 강도 게이트. 중심봉 ATR로 정규화한 좌/우 기울기 중 큰 쪽 ≥ 이 값이어야 통과(미세 프랙탈 탈락). 기본 **0.5**(0~2.0). 0이면 강도 게이트 무효. 전략성 파라미터. |
| `정규화 ATR 길이(atrLen)` | 기울기 정규화용 프랙탈 전용 ATR 길이(중심봉 기준으로 얼림). 기본 **14**(5~50). 비정상/테이블 ATR과 별개. |
| `후보 대칭비 완화 candRelax` | 후보(로직2) 대칭비 하한 = `symMin × candRelax`. 기본 **0.7**(→ 후보 0.35). 낮을수록 후보를 널널하게. 전략성 파라미터. |
| `후보(1봉 조기) 마커 표시` | 확정 1봉 전 후보를 삼각형/역삼각형 마커(offset −1)로 표시. 매수 후보=▲(belowbar)·매도 후보=▼(abovebar). **기본 ON**. 마커는 확정봉(`barstate.isconfirmed`)에서만 표시(실시간 점멸 없음). 표시 전용. |
| `매수(하방 프랙탈) 색` / `매도(상방 프랙탈) 색` | 확정 프랙탈 삼각형(불투명) + 후보 삼각형/역삼각형 마커(투명도 50) 색. 기본 **파랑 / 빨강**. 후보는 이 색을 공유합니다. |
| `후보 마커 투명도` | 후보 삼각형/역삼각형 마커 투명도(0~100). 기본 **50**. 색은 확정 프랙탈 색 공유·이 투명도만 다르게 적용해 확정 마커와 구분. 후보 표시 ON일 때만 유효(`active`). ⚠️ 구 `매수 후보 배경색`·`매도 후보 배경색`·`후보 배경 투명도(fracCandBgTransp)` 3종은 후보 배경 제거(본부 지시)로 **삭제**됐습니다. |
| `%R 다이버전스 원 표시` / `윌리엄스 %R 길이` / `%R 하단·상단 밴드` | 윌리엄스 %R 다이버전스 원(강세=녹·belowbar / 약세=노랑·abovebar) 표시·`ta.wpr` 길이(기본 14)·밴드(기본 −80/−20). **기본 ON**. 표시 전용. |
| `%R 강세·약세 다이버전스 원 색` | %R 다이버전스 원 색(강세 매수=녹 #66BB6A / 약세 매도=노랑 #FFEB3B). 표시 ON일 때만 유효(`active`). |
| `Hide VWAP on 1D or Above` | 일봉 이상에서 선을 숨기고 싶을 때 |

## 주의사항

- 이 지표는 거래량 데이터가 있어야 정상 동작합니다. 코드에는 거래량이 없을 때 `runtime.error`도 들어 있습니다.
- VWAP 누적은 **Recent Bars 롤링 단일 방식**으로 고정입니다(앵커모드·세션/거래일 기준 제거).
- `Recent Bars`는 차트 타임프레임의 봉 수 기준이라, 타임프레임을 바꾸면 같은 숫자여도 의미가 달라집니다.
- 저유동 종목에서는 VWAP과 밴드 반응이 더 거칠 수 있습니다.
- 성능: 헤더에 `max_bars_back=1000`을 두어(원본 이치모쿠와 동일 헤드룸) `ta.vwma(close, 200)`+σ 등 200봉 참조의 런타임 오프셋 오류·버퍼 자동확장을 예방합니다(산식 변경 아님).
