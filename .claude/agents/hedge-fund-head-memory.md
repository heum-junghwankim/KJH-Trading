# 투자본부장 운영 메모리 (Head of Investment — Operating Memory)

> 목적: 본부장이 세션 시작 시 참조하는 조직 운영 메모리. 조직 상태·위임 규칙·미결 안건을 기억한다.

## 🚫 제1원칙 — 실무는 절대 직접 수행하지 않는다

- 본부장은 **CEO의 명령을 본인이 직접 손으로 수행하지 않는다.** 코드 수정, 파일 편집·이동·리네이밍, README 작성, 백테스트·계산·리서치 등 **모든 실무는 반드시 해당 부서 에이전트에게 `Agent` 툴로 위임**한다.
- 본부장이 직접 하는 일은 오직: ① CEO 지시 해석 ② 부서에 작업 지시 ③ 결과 종합·검증 ④ CEO 보고·권고.
- 유일한 예외: `.claude/agents/**` 조직 정의(페르소나·메모리)의 관리 같은 **본부 운영 행위**. 펀드 자산(`pinescript/**`, `README.md`)·리서치·트레이딩 실무는 예외 없이 위임.
- "간단하니 바로 하겠다"는 유혹 경계 — 작아 보여도 위임한다.

## 조직도 — 현재 구현된 에이전트

| 부서 | 에이전트 | 상태 | 위임 대상 업무 |
| --- | --- | --- | --- |
| 스크립트 개발자 | `script-developer` | ✅ 구현 | Pine `.pine` 코드 개발·최적화·리팩토링, 지표 UI, README |
| 알고리즘 수학자 | `quant-mathematician` | ✅ 구현 | 신호의 확률적 엣지 검증, 백테스트·성과 분석, 개선 아이디어(→개발자) |
| 애널리스트 | (미구현) | ⬜ | 매크로·국면·종목 리서치 |
| 숏 데이/스윙 트레이더 | (미구현) | ⬜ | 숏 포지션 — 추세 숏 + 역추세 숏 |
| 롱 데이/스윙 트레이더 | (미구현) | ⬜ | 롱 포지션 — 추세 롱 + 역추세 롱 |
| 롱숏 중장기 펀드 매니저 | (미구현) | ⬜ | 중장기 롱숏 — **오로지 추세 추종만** |

## 협업 라인

- 개선 흐름: **알고리즘 수학자(근거·검증) → 본부장(리스크·과최적화 검토) → 스크립트 개발자(구현)**.
- 코드 이해의 출발점: `script-developer-memory.md` (개발자 코드베이스 지식 메모리).

## 코드베이스 알려진 리스크 (개발자 학습 결과, 커밋 4da4c85 기준)

1. **Pine 버전 혼재** — cRSI=**v6(정정, 기존 'v4'는 오기)**, MACD·OBV-ADX·이평선 리본=v5, 나머지 v6. 수정 전 파일별 버전 확인 필수.
2. **리페인팅 노출** — MACD `request.security`(상위 TF), 이치모쿠 예측모드/캔들 실시간 프리뷰. 리페인팅 수정은 신호 로직 변경이므로 별도 전략 검토 사안. auto-vwap **정렬 삼각형**(구 전환후보 삼각형 폐기)은 현재봉[0]이 리본 `line[0]` 포함이라 **의도된 리얼타임 리페인팅**([1..4] 확정봉 고정, 참고용·CEO 승인).
3. **성능 한도** — cRSI 동적밴드 봉당 O(2×101×cyclicmemory)≈8천 루프(break 조기종료로 실측은 낮음), 캔들 파일 라벨/박스 500 상한 근접, 이치모쿠 플롯 다수. auto-vwap에 cRSI 이식 시 이중루프를 `triLookback`(200봉) 게이트로 한정해 페인팅 유지.

## 미결 안건 (CEO 판단 대기)

- **✅ %R 원 색상 + 밴드 배경 밴드워킹 교체 세션 (2026-08-27, 브랜치 `feat/auto-vwap-slope-display-mode`, HEAD `2cff77d`, origin 푸시 완료·main 미병합·main 대비 +26/-0 fast-forward 가능):**
  - ① **%R 다이버전스 원 기본색 교체**: 아래 원(매수·belowbar) `#00FF00`→`#00E676` 형광 스프링그린, 위 원(매도·abovebar) `#FF9800`→`#FF1744` 형광 핑크레드 (`wprBullColor` L137·`wprBearColor` L138, `input.color` defval만). 1차안(위=앰버 `#FFC400`/아래=마젠타 `#E040FB`, 커밋 `f2f3633`)은 **CEO 반려 — 매수/매도 직관 정합이 시각적 구분보다 상위 요건**. 최종 `61da1e2`. 구분은 계열 변경이 아니라 **명도 격차**로 달성(원=형광 V90~100 vs 삼각형=어두운 채도 V75~78, 배경 녹 V49와 41pt 차). 종전 주황 원의 50선 `#FF6D00` 혼동 리스크도 해소.
  - ② **밴드 배경 페인팅 소스 교체**(`2cff77d`): `bgFillState`(기울기 서열 4모드) → **`bgWalkPaintState = vwUpperWalk ? 1 : vwLowerWalk ? -1 : 0`**(밴드워킹). 색·투명도는 기존 `ribbonBullBgColor`#2E7D32/`ribbonBearBgColor`#C62828/`ribbonBgTransp`70 재사용, **새 input·새 임계 0개**(상태 테이블 `vwap 구간` 행이 쓰는 기존 판정 재사용).
  - **핵심 — `bgFillState` 참조처 5곳 중 페인팅은 1곳뿐**: `fill()`(693, 교체) / `noBg`(삼각형 발동) / `resetBlue`·`resetRed`(삼각형 래치) / `slopeArrColor`(4행 셀색) — 뒤 4곳 **무수정, 삼각형 출현 봉집합·셀색 비트 단위 불변**. 신규 상태값은 fill에서만 소비(역방향 오염 0).
  - **⚠️ CEO 결재 대기 3건**: (A) `bgFilterMode` 4모드 토글 **제거 불가 → 존치**(지우면 삼각형·셀색 붕괴 = 신호 로직 변경, 승인+수학자 검토 사안). UI 라벨 `밴드 배경 필터`가 실기능(삼각형·셀색 제어)과 불일치 → 라벨 정정 여부. (B) **배경 기준(밴드워킹) ≠ 삼각형 억제 기준(`bgFillState`) 비정합** — 통일하려면 신호 변경이라 별도 승인. (C) **스테일 툴팁 8곳**(L111~114, L122~125, L143) 전부 미수정(툴팁 불가침 준수) → 갱신 지시 여부.
  - **구조 조정 1건(값 불변)**: `vwUpperWalk`(943행)가 `fill`(693행)보다 뒤라 선언순서 위반 → **계산 블록만** fill 앞(692~735)으로 이동. `plot`/`fill` 선언 순서 무수정이라 **z-order 불변**.
  - **⚠️ TradingView 실기 미확인**(로컬 컴파일러 없음). 확인 체크포인트 3개: ⓐ밴드워킹 구간에 배경 출현 ⓑ리본 3선이 배경 위 정상 렌더 ⓒ상태 테이블 `vwap 구간`이 "밴드워킹"일 때만 배경. 전부 표시 전용·전략 파라미터 무변경이라 백테스트 에스컬레이션 없음.

- **✅ auto-vwap 로그 기울기 리본 대규모 재설계 세션 (2026-08월, 브랜치 `feat/auto-vwap-slope-display-mode`, 최신 커밋 `60034c5`←`bca0abb`, origin 푸시·main 미병합):** 이전 세션의 **cRSI 필터·전환후보 삼각형·ATR 정규화 모드·프랙탈 신호를 전부 제거**하고 새 구조로 전환. 산출물:
  - ① **로그 기반 기울기 3행** (로그 차트 정합): 1행 TEMA·2행 DEMA·3행 EMA, ㄱ=1봉차분(2점)·ㄴ=3점회귀(과거2+현재)·ㄷ=5점회귀(과거4+현재), OLS 분모 6(3점)/50(5점). 출력 `%`·소수1자리(로직은 2자리). ATR 모드 폐기(로그차트라 불필요).
  - ② **4행 기울기 배열** = TEMA|DEMA|EMA ㄱ값, 셀색=배경필터 상태(bgFillState).
  - ③ **밴드 배경 필터 4모드 토글** `bgFilterMode`: 미적용/정규화 갭/**기울기 크기 게이트(기본값)**/상대 데드존. 파라미터 전부 0.10. 정배열(tema>dema>ema 기울기)=녹 배경/역배열=적 배경. **컨플루언스**: 배경 녹인데 기울기행 셀 1개라도 적이면 녹 억제(반대도).
  - ④ **이격도(T/D·D/E) 2행** (5-6행, 크로스행 대체): 0기준 편차 `(fast/slow−1)×100`, 5봉 시계열(현재봉 왼쪽 [0]…[4]), 1자리·양수부호 생략. **셀색 3단계 CEO 반복 확정**: (a) 부호 기반 5봉 모두 양수=녹/모두 음수=적/혼재·0·na=회색 → (b) 방향 오버라이드: 양수&역배열=어두운파랑#1A237E/음수&정배열=어두운주황#8B4500 → (c) **최종 기준**=현재봉이 과거4봉 대비 최저(td0<=td1..td4)면 파랑/최고면 주황(전체단조·양끝비교 폐기, 현재봉 극값 판정). 혼재봉은 기본 녹/적 유지.
  - ⑤ **정렬 삼각형 재도입**: noBg(배경0)·순 셀카운트(녹−적>=2) 파랑/(적−녹>=2)빨강, size.tiny, %R원 뒤 z-order, 확정봉 래치. **꼬리 필터=순 꼬리 상쇄**(triNetUpper=max(0,윗−아랫)·triNetLower=max(0,아랫−윗); 순방향 꼬리>몸통이면 억제). CEO 발견 버그(3행 녹색인데 파랑 미표출)=양쪽꼬리봉 과다억제 → 순꼬리로 완화 해결.
  - ⑥ **VWAP 중앙선 색 고정**: 지지/저항 동적색(vwapLineColor) 제거 → 흰색 고정(DEFAULT_LINE_COLOR). SR 판정 로직은 미참조로 보존.
  - **⚠️ TradingView 실기 확인 미완**(로컬 컴파일러 없음): 이격도 4색(혼재봉 기본색)·삼각형 순꼬리(3행 녹색봉 파랑)·VWAP선 흰색 육안 검증 후 main 병합 CEO 승인 대기. 전부 표시 전용·전략 파라미터 무변경이라 백테스트 에스컬레이션 없음.
- **auto-vwap ATR정규화·cRSI필터 대규모 세션 (2026-08-14, 브랜치 `feat/auto-vwap-slope-display-mode`, 커밋 `9d8a38b`←`f0ef2d2`←`d22c731`, origin 푸시·main 미병합):** 산출물 — ① **기울기 표시 모드 토글**(기본 각도° / ATR 정규화), 기본값=**ATR 정규화**. 추세확인 1~3행은 모드 무관 **항상 각도° 고정**(정규화는 배경·전환·삼각형 판정에만 적용). ② **ATR 정규화가 밴드 배경·전환 판정까지 전환**(bullAccel/bearAccel, deadZone 0.10·accelMin 0.05, 부호 게이트). **하락배경 미발동 버그 549행 수정**(bearAccel 둘째 갭 `demaNS-emaNS`→`demaNS-temaNS`). 전환행 괄호값 모드무관 각도° 고정. ③ **전환후보 삼각형 마커**(candUp=close>xUp·candDn=close<xDn, bgNeutral 한정, 리얼타임, 꼬리필터, %R 방향확인 억제). **중복억제 래치는 제거됨.** 상승=봉아래 하늘색 `#4FC3F7`·하락=봉위 분홍 `#F06292`. ④ **cRSI 이식(페인트 없음)**: 필터1(밴드위치: 미좁혀짐+cRSI<하단밴드→빨강억제/>상단밴드→파랑억제, `crsiNarrowThreshold=20`)·필터2(3봉 정배열 `crsi[2]<[1]<[0]`→빨강억제). **단일 토글 `useCrsiTriFilter` 기본 ON.** cRSI 기본값 전부 원본 유지(domcycle 20·cyclicmemory 40·vibration 10·leveling 10·phasingLag 4). ⑤ VWAP 중앙선 점선/1px/기본 ON. ⑥ 비정상 캔들: 매도 파랑·매수 붉은 `#E53935`. **실기 미확인(로컬 컴파일러 없음)→ main 병합 전 TradingView 확인 필수.** **cRSI 필터1/2는 신호 억제 로직 변경(관찰지표, 실거래 strategy 무관)이라 수학자 백테스트·과최적화 검토 미실시 — 실전 채택 전 권고.**
- **⚠️ 교훈 — 미커밋 유실 사고:** 위 세션 중 cRSI 동적밴드 초기버전·필터2 B안(4봉차분 `crsi-crsi[4]>2.0`)·CSE·200봉게이트가 **미커밋 워킹트리에만 존재**하다 CEO의 '단순화(실수)' 지시 작업이 덮어써 유실됨(reflog·stash·81블롭 전수 수색 0건). 원본 `rsi-cyclic-smoothed.pine` 산식으로 재이식 복원. → **안정 단위마다 즉시 커밋 필수.** (필터2 최종본은 3봉 정배열; B안 4봉차분은 폐기.)
- **수학자 평가 요약(전환후보 삼각형):** 단독 매매 엣지 없음("투표권 없는 참관인"), 확인용으로만. 강점=신규 임계 자유도 낮음. 약점=단일정보원(EMA삼중재귀)·리페인팅·미검증. cRSI는 리본과 직교하는 독립 모멘텀축이라 단일정보원 약점 완화. 저가/고가 ATR 눌림필터 아이디어는 A안(아래꼬리/몸통, 이미 구현·자유도0)이 우월 → 비채택.
- **변곡 마커 게이트 3봉hlc3→OBV-ADX DI 교체** (브랜치 `feat/auto-vwap-di-gate`, 커밋 `800e2f1`, main 미병합·미푸시): 3봉 hlc3 평균 게이트 제거 → OBV 기반 DI+/DI- 우위 게이트로 교체(숏=DI−>DI+, 롱=DI+>DI−). OBV-ADX(v5) 커스텀 OBV-DI 산식 이식(표준 ta.dmi 아님), input `DI Length=20`·`ADX Smoothing=20`(원본 기본값, 그룹 "OBV DI 게이트"). ADX는 게이트에 미사용이나 원본 정합 위해 계산만 유지(CEO 확인 대기 — 제거 가능). 나머지(후보 3모드 OR·수평필터·밴드워킹·마커 원형/위치/색) 불변. 리페인팅 없음. TradingView 실기 확인·병합 CEO 승인 대기.
- **✅ 완료·병합됨 — 변곡 마커 세션 전체가 `main`에 병합·푸시됨** (병합 커밋 `3ae5441`, `main=origin/main` 동기). auto-vwap 변곡 마커(3모드 OR 후보·모드독립 수평필터·3봉 hlc3 게이트·밴드워킹 억제) + 스윕 기본값(기본/10/2.0/0.5) + 이치모쿠 지표 손익비 행·샹들리에 제거. **로컬 Pine 컴파일러 부재로 TradingView 실기 컴파일 미확인 — 로드 확인 권고.** 실거래 전략 `ichimoku-strategy.pine`은 세션 내내 미변경(스윕 기본값 통일 미적용, 필요 시 수학자 백테스트 선행). 조직 메모리 파일은 여전히 워킹트리 uncommitted(펀드 코드와 분리).
- **auto-vwap 변곡 지점 업데이트** (브랜치 `feat/auto-vwap-inflection-cross`, HEAD `22cbc68`): 밴드2 색변경 제거·1px, 변곡 십자가 마커(청산선 색), VWAP 중앙선·청산선 1px, 밴드워킹 수평 필터(ATR 정규화, 기본 임계 0.10) 완료. CEO 지시로 **단순화**: 변곡 전용 VWAP/청산선 세트 제거 → 기본 세트 하나로 통합, 변곡 판정·마커는 기본 밴드2+기본 청산선 기준. 도형 이슈 수정(`location.top/bottom`→`location.absolute`+`size.tiny`로 밴드2 값 좌표에 정확 렌더). VWAP 평행선(현재값 수평선) 제거. **표시 전용·전략 로직 불변.** → **TradingView 실기 컴파일·마커표시 확인 후 푸시·main 병합** CEO 승인 대기. 로컬 Pine 컴파일러 부재로 정적 리뷰만 수행.
- **이치모쿠 테이블 VWAP 변곡 편입 → 롤백됨** (커밋 `3ab7598` 편입 후 CEO 지시로 `215fe7a`에서 `git revert`). 현재 이치모쿠(`advanced-ichimoku.pine`)는 편입 이전(`22cbc68`)과 바이트 동일 — VWAP 변곡 없음. (재도입 필요 시 3ab7598 참조.)
- **브랜치 원격 푸시**: `feat/auto-vwap-inflection-cross` → `origin`(업스트림 설정). 단 이후 로컬에 신규 커밋 있어 원격보다 앞섬(아래).
- **변곡 게이트 재설계** (로컬 HEAD `65b90b2`, 미푸시): 베이스라인 게이트 제거 → KC 게이트(`29687db`) 시도했다 CEO 철회 → 최종 "청산선 수평화 시작봉 고가/저가" 게이트로 확정. 숏=현재고가<수평화시작봉고가, 롱=현재저가>수평화시작봉저가(`refHighShort`/`refLowLong` var latch). 게이트 제거(`97f6a68`) → CEO 재지시로 **누적 극값 게이트 재도입**(로컬 HEAD `7509f79`). 최종 마커식 = 밴드2 청산선 진입판정 + 수평 필터 + 밴드워킹 억제 + **수평화 후 누적극값 게이트**(숏: 현재고가<수평화구간 직전누적최고가, 롱: 현재저가>직전누적최저가; var latch, 현재봉 반영 전 비교 후 갱신). 수평 임계 `ipFlatSlopeMax` 0.10→**0.15** 완화. 마커 원형·y좌표(기본 밴드2)·색 유지. 베이스라인·KC 없음. 이후 스윕 청산 기본값 통일(`cd4fc05`): 스윕 가격 모드="최근봉 가중", 가중 트레일 ATR 배수=2, 청산 버퍼 ATR 배수=1 — auto-vwap 기본3+변곡3 및 **이치모쿠 지표(advanced-ichimoku)** 에 적용. 게이트 소스 hlc3→고가/저가 롤백(`77caaff`). 이후 **변곡 마커 로직 전면 재설계**(HEAD `8060b78`): 설정 3그룹(①기본 vwap 50 ②변곡용 vwap 22 ③청산 단일)으로 정리, **변곡용 청산 전용 설정 삭제**. 후보=기본 청산선을 기본·거래량가중·최근봉가중 **3모드 계산 → 밴드2가 1개 이상 진입 시 후보**(computeClearLines 4회 호출=화면1+후보3). 표시 게이트=숏 현재고가<3봉 고가평균/롱 현재저가>3봉 저가평균(누적극값 게이트 대체). 수평 필터(기본 청산선 기준)·밴드워킹 억제 둘 다 유지. 최종식=후보 AND 수평필터 AND 3봉평균 AND 밴드워킹억제. 마커색=기본 청산선 색. 이후 **모드 누수 버그 교정**(`69cb62e`): 수평 필터가 화면 페인팅용(ceMode) 청산선을 쓰던 것을 "후보에 기여한 모드의 청산선 flat"로 전환 → `ceMode`(스윕 가격 모드)는 화면 페인팅 전용, 마커 경로(후보·flat·표시)에서 완전 분리. 마커식 = 후보(3모드 OR) AND 모드독립 수평필터 AND 3봉평균 게이트 AND 밴드워킹억제. 이후 3봉 게이트를 **hlc3 단일 기준**으로 교정(`a0cc437`): 숏 gateHlc3<avg3Hlc3 / 롱 gateHlc3>avg3Hlc3 → 롱·숏 상호배타(과다발동 정리). 이후 스윕 청산 기본값 CEO 확정 적용(`d7945a8`, 양 지표): `ceMode`="기본", `ceWeightLen`=10, `ceWeightMult`=2.0, `sweepBufferMult`=**0.5**(1.0→0.5, 발동 소폭↑). 수학자는 합3.0 유지(2.5+0.5) 권고했으나 CEO는 합2.5(2.0+0.5) 선택 — 감도 우선. 이치모쿠 strategy 파일(`ichimoku-strategy.pine`)은 여전히 옛 기본값(미변경). 이후 **이치모쿠 지표에서 손익비 현황 행 전체 + 샹들리에 청산선 로직 제거**(`7fe4182`): [손익비] 섹션(Cha 롱/숏·Ret 롱숏) + computeClearLines·longClear/shortClear·스윕/청산 input 그룹·f_volRecencyMa·cha*/ret RR 헬퍼 제거(168줄 삭제). 청산선은 손익비 표시 전용이라 안전 제거(전략·신호 불변). 이격도/되돌림 로직은 다른 행(이격도·되돌림가)에도 쓰여 보존. table.new 24→20행, row 0~19 무결성 검증 완료. 참고: 이 제거로 `d7945a8`에서 바꿨던 이치모쿠 지표 스윕 기본값도 함께 사라짐(auto-vwap 스윕 기본값은 유지). HEAD=`7fe4182`. 원격은 아직 `215fe7a`(로컬 앞섬) — 푸시·병합 CEO 승인 대기. TradingView 실기 확인 필요.
- **⚠️ 미결/리스크 — 이치모쿠 전략 파일 기본값 불일치:** 청산선 스윕 기본값은 **지표 파일(advanced-ichimoku, 표시·Cha손익비 전용)에만** 반영됨. **`ichimoku-strategy.pine`(strategy, 실거래 손절선 `tfStopPrice:=shortClear/longClear`)는 옛 기본값(기본/1.5/0.4) 유지** — CEO가 지표만 지시했기에 미변경. 결과적으로 지표 표시와 전략 실거래 세팅이 달라짐. 전략 파일까지 통일하려면 **매매 결과가 바뀌는 전략 변경**이므로 CEO 승인 + 수학자 백테스트 선행 필요.
- **메인 축 정합성:** 페르소나상 메인은 이치모쿠. 단, 비정상 가격 추적(캔들)을 레거시로 이관하는 결정으로 자연 정리 진행 중 → 이치모쿠 메인으로 수렴 예정.

## 워크플로 규칙 (CEO 지시, 2026-08-27 — 항구 적용)

- **🔒 커밋 금지 — CEO의 명시적 "커밋 & 푸시" 지시가 있을 때까지 어떤 부서도 커밋하지 않는다.** (CEO 직접 지시, 2026-08-27)
  - 작업은 **워킹트리에만** 남긴다. `git commit` 금지, `git push` 금지, 태그 금지.
  - 위임 시 **매번 이 제약을 명시**할 것. 개발자가 습관적으로 "안정 단위 즉시 커밋"하지 않도록 지시서에 못 박는다.
  - ⚠️ **이 규칙은 종전 "안정 단위마다 즉시 커밋" 수칙을 대체·무효화한다.** 상충 시 CEO 지시가 우선.
  - ⚠️ **리스크(본부 고지 완료):** 미커밋 유실 사고 전례 있음(cRSI 초기버전·필터2 B안·CSE·200봉 게이트 통째 유실, reflog·stash 수색 실패). 커밋 금지 기간이 길어질수록 유실 노출이 커진다. **본부는 작업 단위가 끝날 때마다 CEO께 커밋 승인을 요청**해 노출 시간을 줄인다.


- **🚫 별도 피처 브랜치 생성 금지.** 모든 작업은 **`main` 브랜치에서 직접** 수행한다. `git checkout -b` 금지.
  - 배경: CEO가 브랜치 분기의 실익을 인정하지 않음("왜 별도 피처 브랜치를 만들었는지 모르겠다"). 미병합 브랜치 누적 → 검증 지연·병합 부채가 실제로 발생했음.
  - 위임 시 매번 이 규칙을 명시할 것. 개발자 메모리에도 기록 지시 완료.
- **main 직접 작업이므로 안전 수칙이 더 중요해진다:**
  - **안정 단위마다 즉시 커밋** (미커밋 유실 사고 재발 방지).
  - **푸시는 CEO 승인 시에만.** main 작업이라고 자동 푸시 금지.
  - 전략 파라미터(임계·기간) 무단 변경 금지, 툴팁 불가침.
- **✅ main 병합·푸시 완료 (2026-08-27):** `feat/auto-vwap-slope-display-mode`(`2cff77d`) → `main` **fast-forward 병합 후 origin 푸시**(`08cf81c..2cff77d`, 26커밋 유입, 충돌 0, force 미사용). 현재 `main`=`004acce`(개발자 메모리 규칙 커밋 1건 ahead, 미푸시). 체크아웃=main.
- **⚠️ 메모리 사실 정정 — DI 게이트는 이미 main에 있었다:** 종전 메모리는 `800e2f1`(변곡 마커 게이트 3봉 hlc3→OBV DI+/DI− 우위 교체)을 "main 미병합"으로 기록했으나, **실제로는 이번 병합 이전부터 `main`·`origin/main`의 조상**이었음(`git branch --contains`·`merge-base --is-ancestor` 확인). 즉 **미검증 신호 로직(DI 게이트)이 이미 main에서 가동 중**이며, 배제하려면 revert가 필요하고 이는 신호 로직 변경=CEO 승인 사안. `origin/feat/auto-vwap-di-gate` 브랜치 팁은 별개(`868664d`, triple-stoch 표기)로 미병합 보존.
- **잔존 미병합 원격 브랜치 9개** (`feat/auto-vwap-cleanup`, `feat/auto-vwap-signal-suite`, `feat/ichimoku-*` 등) + `feat/auto-vwap-di-gate`. 신규 분기는 금지됐으나 **기존 브랜치 처리(병합/폐기) 결재 미결** — 방치 시 병합 부채 누적.

## 저장소 정리 (2026-08-27, CEO 지시)

- **`main` = `e57a674`, origin 완전 동기.** 로컬 브랜치는 **`main` 단독**(피처 브랜치 전량 로컬 삭제 완료).
- **전수 조사 결과 — 피처 브랜치 10개 전부 `main`에 완전 병합, main에 없는 고유 커밋 0건.** 즉 "모두 병합" 지시는 이미 충족 상태였고, 본부가 우려한 회귀 리스크는 **실재하지 않았음**(병합할 커밋 자체가 없음). → 교훈: **브랜치 상태는 추정하지 말고 먼저 조사시킬 것.**
- **백업 태그 9개 origin 푸시 완료** (`backup/feat/*`, annotated, 복원 리허설 검증). 브랜치 소멸 후에도 커밋 영구 보존. 복원 = `git branch feat/<이름> backup/feat/<이름>`.
- **⏸ 미완 1건 — 원격 브랜치 9개 삭제가 권한 시스템(classifier)에 차단됨.** 백업·검증은 끝나 준비 100%. 해소 경로: ⓐ CEO가 직접 `git push origin --delete <브랜치>` 실행, ⓑ Bash 권한 규칙 추가(설정 변경은 CEO 승인 사안). 개발자는 `gh api` 우회를 **의도적으로 시도하지 않음**(차단 취지 존중) — 올바른 판단.

## 원칙 요약

- 결론(권고) 먼저 → 근거 → **리스크(최대 손실/손절)** → 대안.
- 단일 신호 불신, 교차 검증(이치모쿠 + 테이블 + 보조지표 정렬).
- 자본 보존 우선, R:R 미확보 진입 불권고.
- 전략값(임계·기간)은 검증·승인 없이 변경 금지.
- 부서 출처 명시, 한국어 기본·트레이딩 용어 원어.
- **🚫 툴팁 임의 수정 금지 (CEO 지시, 2026-08):** 개발자·부서는 **CEO/본부의 명시적 지시 없이 테이블 툴팁 문구를 변경·추가·삭제하지 않는다.** 툴팁 내용은 CEO가 직접 관리하는 영역이다. 다른 작업(로직·버그 수정 등) 중 "문서 동기화" 명목으로도 툴팁을 임의로 손대지 말 것. 위임 시 이 제약을 매번 명시하고, 개발자가 툴팁을 건드리면 롤백한다. (사고: X≤0 가드 제거 작업 중 개발자가 tdCrossTooltip에 "강추세" 문구를 임의 추가 → CEO 지적 → 롤백.)
